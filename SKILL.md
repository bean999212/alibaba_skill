---
name: aladdin-ione-daily-test-report
description: 根据用户输入的需求名称或测试计划名称进行全局模糊匹配；若输入为纯数字 ID，则按 ID 模糊搜索阿拉丁与 ione 平台上的需求及阿拉丁测试计划。每次执行时必须基于用户本次输入重新搜索，禁止使用历史报告元数据、缓存文件或过往会话中保存的 ID 作为搜索入参。经用户确认后拉取用例测试执行进度与缺陷数据，结合项目发布时间评估测试风险；生成日报后通过 DWS 拉取钉钉群列表并让用户多选目标群，支持固定时间或连续多日的定时发送。Use when the user needs daily test progress reports, release risk assessment, requirement-based data retrieval, DingTalk group messaging, or integration of Aladdin and ione platform data.
version: 1.0.0
---

# 阿拉丁 + ione 每日测试风险日报

## 用途

本 Skill 用于根据用户输入的需求名称、测试计划名称或需求 ID，在阿拉丁、ione、Aone 平台上模糊匹配对应需求、项目与测试计划。输入为纯数字时按 ID 维度搜索，输入为文本时按名称维度搜索；若 Aone 项目或阿拉丁测试计划未匹配到，会先按同义词表扩展关键词再次搜索，仍未命中时再反馈给用户并允许手动输入对应 ID。经用户确认后拉取用例执行进度与缺陷数据，结合项目发布时间评估风险、生成日报；当阿拉丁测试计划无法获取时，还支持用户粘贴钉钉文档或语雀文档链接作为测试进度来源。随后通过 DWS 拉取用户钉钉群列表并让用户多选目标群，支持固定时间或连续多日的定时发送。完整流程如下：

1. 接收用户输入的需求名称、测试计划名称或需求 ID。
2. 在阿拉丁平台与 ione 平台分别模糊搜索匹配的需求；同时在 Aone 平台搜索匹配的项目，在阿拉丁平台搜索匹配的测试计划。搜索范围覆盖**近一个月内**的全部项目与测试计划，且必须**穷尽全局搜索**（分页 / 滚动翻到底），不得搜到第一个项目或测试计划就停止；只有在全部结果都被收集完毕后才结束搜索。一个项目名称常会命中多个项目或多个测试计划，需将命中的**全部**项目与测试计划返回给用户。
3. 若 Aone 项目或阿拉丁测试计划首次搜索未匹配到，按内置同义词表对用户输入进行扩展并再次搜索；若扩展后找到候选，展示给用户确认。
4. **分两步顺序确认**：先把全局搜索到的**全部** Aone 项目候选返回给用户确认（支持多选）；待 Aone 项目确认完成后，再把**全部**阿拉丁测试计划候选返回给用户确认（支持多选）。若某维度经同义词回退后仍未匹配到，提示用户手动输入对应 ID 或重新搜索。
5. 两步均确认后记录所选标识（`aone_project_ids`、`aladdin_test_plan_ids` 等均以列表形式记录，支持对多项目、多测试计划一起拉取数据并生成日报）。**确认完成后，Agent 必须自动、连续地执行下面第 6–13 步，中途不得停下或反问「是否继续」；确认之后仅允许两处必要停顿：第 11 步钉钉群多选、第 12 步发送方式选择。**
6. 根据确认后的一个或多个阿拉丁测试计划 ID 拉取并汇总用例执行进度；若无法获取测试计划，可让用户提供钉钉文档或语雀文档链接作为测试进度数据来源。
7. 根据确认后的一个或多个 Aone 项目拉取缺陷数据并合并统计。
8. 根据项目发布时间与测试完成度自动评估风险。
9. 生成测试日报。日报必须同时产出两种格式：
   - **HTML 格式**：保留为本地文件（默认位于 `outputs/`），便于离线查看、邮件附件或后续二次处理。
   - **钉钉文档格式**：通过 DWS 创建/更新为在线钉钉文档，便于团队协同、评论与分享。
   两种格式最终都要提供给用户，由用户按需选择使用。
10. 拉取用户钉钉群列表（如未授权则引导授权）。
11. 让用户多选需要发送的钉钉群。
12. 询问发送方式：立即发送、固定时间发送或连续多日发送。
13. 执行发送或创建对应的定时任务。

## 前置条件

数据访问**不需要任何 Token / API Key**，全部走已授权通道：

- 阿拉丁 / AAT 用例执行进度：浏览器（builtin_browser）已登录 AAT，可打开计划页 `https://aat.alibaba-inc.com/page/jointDetail?...` 并读取渲染内容。
- Aone 缺陷 / 项目：已安装并授权 coop MCP 连接器（`search_workitem_by_project`、`search_project_by_project_name`、`get_project_info_by_project_id` 等）。
- 钉钉群列表 / 消息 / 文档：已安装并授权 QoderWork 的 DingTalk Workspace（DWS）连接器，允许读取群列表、发送群消息、读写钉钉文档。
- 已填写 `config.yaml`（配置模板见 [config.template.yaml](config.template.yaml)，仅含匹配/风险/报告/调度等非凭证参数，详细说明见 [reference.md](reference.md)）。

## 配置速览

执行前确保 `config.yaml` 至少包含：

```yaml
# 数据源均走已授权通道，无需 Token / API Key
sources:
  aat_via_browser: true    # 阿拉丁 / AAT：builtin_browser 已登录 tab
  aone_via_coop: true      # Aone：coop MCP 连接器
  dingtalk_via_dws: true   # 钉钉：DWS 连接器

aone:
  project_search_enabled: true

matching:
  # 匹配策略：contains / similarity / both / slice
  # 名称跨中英文、词序可能颠倒、易缺字时，优先用 slice（切片匹配），召回率最高
  strategy: slice
  min_similarity: 0.6
  slice:
    cn_gram: 2            # 中文按 n-gram 切片（建议 2；用 1 会因单字命中而大量误召）
    require_all_ascii: true  # 查询里的英文/数字片必须全部命中
    min_cn_hits: 1       # 中文 n-gram 片至少命中的数量
  max_candidates: 5

report:
  title: "测试日报"
  timezone: "Asia/Shanghai"
  fallback_to_file: true
  output_dir: "./outputs"

schedule:
  default_time: "09:00"
  default_consecutive_days: 5
```

## 执行流程

### 1. 输入需求名称或 ID

由用户提供待查询的需求名称、测试计划名称或需求 ID，例如：

> 会员积分兑换

或

> 2117281

### 2. 模糊匹配需求、Aone 项目与阿拉丁测试计划

**搜索原则（强制）：**

- **禁止以本地脚本或文件作为数据源**：Agent 不得以任何理由读取工作区内的本地脚本（如 `generate_report.py`）、本地缓存（如 `aone_bugs_summary.json`、`report_data.json`、`report_meta.json`）或任何其他本地文件来替代平台实时数据。日报所需的缺陷、测试计划、执行进度必须从 Aone/coop、阿拉丁/AAT、ione 等已授权通道实时拉取；仅 Skill 内置的 `assets/` 静态模板/资源允许本地读取。
- **禁止溯源历史数据**：Agent 不得以任何方式使用历史日报中的 `aone_project_id`、`aladdin_test_plan_id`、`ione_req_id`、`aladdin_req_id`、报告元数据（`report_meta.json` 等缓存文件），或过往会话/记忆中保存的项目/计划/需求 ID 作为本次搜索的入参。
- **每次均全局重搜**：无论用户输入的是名称还是 ID，也无论该需求/项目是否曾在以往会话中生成过日报，都必须使用用户本次输入作为唯一依据，重新在阿拉丁、ione、Aone 各平台进行全局模糊搜索。
- **穷尽搜索、禁止提前停止**：搜索时必须翻遍全部结果——Aone 项目按 coop 分页参数（`page` / `perPage`）逐页拉取直到返回空页；阿拉丁 / AAT 页面须滚动或翻页到底，加载出全部结果。**绝不允许命中第一个项目 / 测试计划就停止搜索**，必须在全局搜索完成、确认没有更多结果后才结束。
- **限定近一个月范围**：搜索范围限定为**近一个月内**（当前日期往前推 1 个月）创建或更新的项目与测试计划，避免遗漏近期新建项目，同时排除过于久远的历史项目。Aone 侧可用 `createdAtFrom` 设为一个月前的带时区 ISO8601 时间；阿拉丁 / AAT 侧按页面时间筛选或对结果按时间过滤。
- **新增需求/计划兼容**：若用户输入对应的是新创建、刚变更或尚未沉淀到历史数据的需求/计划，应通过全局模糊搜索与同义词扩展来识别；搜索未果时进入手动输入流程，绝不可回退到历史数据源。
- **先确认再拉取**：只有在把全部搜索结果返回给用户、并得到用户明确确认（勾选具体项目 / 测试计划）之后，才能进入数据拉取环节；未经确认不得擅自对任意项目 / 计划拉取数据。

接收用户输入后，先判断输入类型：

- 若输入为**纯数字**（例如 `2117281`、`TP-1001` 中的数字部分，或仅由数字组成），则按 **ID 维度**在各平台搜索。
- 若输入为**文本**（包含中文字符、英文字母、横线、下划线等非纯数字内容），则按 **名称维度**在各平台搜索。

同时在以下维度搜索：

- **阿拉丁需求**：用于关联后续缺陷与测试计划。
- **ione 需求**：用于拉取缺陷数据。
- **Aone 项目**：用于生成 Aone 缺陷链接与项目归属。
- **阿拉丁测试计划**：用于拉取用例执行进度。

**多结果全量返回 + 分两步确认（强制）：** 一个项目名称在 Aone 或阿拉丁上常会命中**多个项目或多个测试计划**（例如同一战役拆分为多个站点/子项目/多轮测试计划）。搜索时必须先**穷尽近一个月内的全部结果**（分页 / 滚动到底后再停止），不得命中一条就返回；无论最终命中多少条，都必须把**全部**匹配结果返回给用户确认，禁止只返回第一条或擅自替用户挑选。确认按**两步顺序**进行，且都支持**多选**：**第一步先返回全部 Aone 项目候选让用户多选确认；待 Aone 项目确认完成后，第二步再返回全部阿拉丁测试计划候选让用户多选确认**，不要一次性把项目和测试计划混在一起展示。两步都确认后，才对所选的多项目、多测试计划分别拉取并汇总数据。**只有在用户确认之后才拉取数据。**

搜索方式：

- **阿拉丁需求 / ione 需求**：
  - 用 builtin_browser 打开已登录的阿拉丁 / ione 页面，按名称或 ID 在页面内搜索、读取候选（内网认证页无法用 WebFetch，须用已登录 tab）。
- **Aone 项目**：
  - 文本输入：调用 coop `search_project_by_project_name`，按项目名称模糊匹配；**必须逐页翻取（`page` 从 1 递增、按 `perPage` 拉取）直到返回空结果，把近一个月内命中的所有项目全部收集齐，禁止只取第一页或第一条**；若首次无候选，按 `matching.synonyms` 同义词表扩展关键词后再次搜索。
  - 数字输入：优先将输入值作为 Aone 项目 ID 直接查询（如 `get_project_info_by_project_id`）；若接口返回不存在或项目已归档，可再将该 ID 作为关键词调用 `search_project_by_project_name` 进行二次匹配，仍未命中时允许用户手动输入完整项目 ID。
- **阿拉丁测试计划**（模糊匹配、多结果）：
  - **必须使用「测试计划列表页」`https://aat.alibaba-inc.com/page/jointList?tabKey=tp&tid=26` 做搜索入口**：用 builtin_browser 打开已登录的该列表页，在查询表单的「测试计划名称」输入框填入用户项目名称做**模糊匹配**，点击「查询」，把「每页显示」切到 50，**逐行读取结果表并翻页到底**，加载出近一个月内命中的全部测试计划后再停止，**禁止只读取首屏第一条**。
  - **禁止**把「计划详情页」`https://aat.alibaba-inc.com/page/jointDetail?tabKey=30&id=<planId>&classify=tp` 当搜索入口——它是单个计划的详情页，永远只返回 1 条，是过去「只搜到一个」的根因；详情页仅用于查看某个已确定的计划。
  - 若首次无候选，按 `matching.synonyms` 同义词表扩展关键词后再次在列表页查询。搜到的全部计划（id+名称+测试周期）原样返回给用户多选确认。

**全部返回（强制）：** 无论哪个维度（阿拉丁需求 / ione 需求 / Aone 项目 / 阿拉丁测试计划），全局模糊搜索命中的**所有**结果都必须原样返回给用户确认，不做数量截断、不预先过滤、也不替用户挑选。`max_candidates` 仅可用于结果的展示排序，**不得用于截断候选**；确保用户能看到并勾选全部模糊匹配到的内容。

**切片匹配（`strategy: slice`，推荐默认，优先召回）：** 项目名称常跨中英文、词序颠倒或缺字（例如查询「中文版bonus」，真实计划名叫「bonus中文版-逆向」「中文bonus正向(...)」），此时 `contains` 会因严格子串漏掉、`similarity` 会因括号/后缀噪声稀释比值同样漏掉。切片匹配的判定方式：把**查询**切成「英文/数字连续片」（如 `bonus`）与「中文 n-gram 片」（`cn_gram` 默认 2，如 `中文`、`文版`）；某候选名称**同时满足**「查询里的英文/数字片全部作为子串命中」（`require_all_ascii`）且「命中的中文 n-gram 片数量 ≥ `min_cn_hits`（默认 1）」即判为命中，**词序无关**。中文务必用 2-gram，不要降到单字——单字会把仅含「中」等公共字的无关计划大量误召。本策略以召回优先，命中结果同样全部返回给用户确认，由用户勾选。

### 3. 用户确认与未匹配处理（分两步顺序确认）

候选结果**不一次性合并展示**，而是按「**先 Aone 项目、后阿拉丁测试计划**」的顺序分两步反馈给用户；每一步都支持**多选**，且必须等当前步骤确认完成后再进入下一步。

#### 第一步：确认 Aone 项目（多选）

先把全局搜索到的**全部** Aone 项目候选返回给用户确认（此时**不**展示测试计划），例如：

```
已在 Aone 全局搜索到以下项目，请确认要纳入日报的项目（可多选，输入多个编号如 1,3；也可重新输入关键词或手动输入项目 ID）：
1. 会员积分兑换-菲律宾（ID: 2158868）
2. 会员积分兑换-印尼（ID: 2158869）
3. 会员积分兑换-泰国（ID: 2158870）

同时匹配到的 ione 需求（用于拉取缺陷，可多选）：
1. 【会员积分兑换】功能缺陷（ID: I-2001，状态：进行中）
2. 会员积分兑换-退款场景（ID: I-2002，状态：已完成）
```

用户确认后记录 `aone_project_ids`（列表，可含多个项目）与 `ione_req_ids`（列表）。**只有本步确认完成后，才进入第二步；在用户回复确认之前不得展示测试计划，也不得开始拉取数据。**

#### 第二步：确认阿拉丁测试计划（多选）

在 Aone 项目确认完成后，再把全局搜索到的**全部**阿拉丁测试计划候选返回给用户确认，例如：

```
Aone 项目已确认。以下是在阿拉丁全局搜索到的测试计划，请确认要纳入日报的测试计划（可多选，输入多个编号如 1,2；也可重新输入关键词、手动输入计划 ID，或粘贴钉钉/语雀文档链接作为测试进度来源）：
1. 会员积分兑换-功能测试（ID: TP-1001）
2. 会员积分兑换-回归测试（ID: TP-1002）
3. 会员积分兑换-兼容测试（ID: TP-1003）

关联的阿拉丁需求（可多选）：
1. 会员积分兑换功能（ID: A-1001，负责人：张三）
2. 会员积分兑换优化（ID: A-1002，负责人：李四）
```

用户确认后记录 `aladdin_test_plan_ids`（列表，可含多个计划）与 `aladdin_req_ids`（列表）。

两步都确认后，Agent 汇总以下标识作为后续拉取数据的依据（**项目与测试计划均为列表，支持多项目 / 多计划一起拉取**）：

- `aone_project_ids`：第一步选中的 Aone 项目 ID 列表（可含多个项目）。
- `ione_req_ids`：第一步选中的 ione 需求 ID 列表。
- `aladdin_test_plan_ids`：第二步选中的阿拉丁测试计划 ID 列表（可含多个计划）。
- `aladdin_req_ids`：第二步选中的阿拉丁需求 ID 列表。

> 单选场景为列表长度为 1 的特例；生成缺陷链接时按每条缺陷所属的项目 ID 分别拼接，不假定只有一个项目。

#### 确认后连续执行（强制）

**一旦用户完成上述两步确认（Aone 项目 + 阿拉丁测试计划），Agent 必须自动、连续地执行完剩余全部步骤，中途不得停下、不得反问「是否继续」、不得等待用户再次催促。** 后续步骤按顺序一气呵成：拉取数据（第 4 步）→ 计算风险（第 5 步）→ 生成报告（第 6 步，HTML + 钉钉文档）→ 拉取钉钉群列表让用户多选目标群（第 7 步）→ 按所选发送方式立即发送或创建定时任务。

确认之后**仅允许**两处必要停顿，均为发送环节的必需交互：（a）钉钉群多选确认；（b）发送方式选择（立即 / 固定时间 / 连续多日）。除此之外不得中断。

若某数据源暂时不可用（AAT 登录态失效、coop 未返回、测试计划取不到等），**按既定降级规则处理**（请用户提供钉钉/语雀文档链接作为测试进度来源、在对应单元格标注「数据来源异常」等），继续把日报生成到底，而不是停下等待——绝不因单一数据源缺失就中止整个流程。

#### 未匹配到项目或测试计划时的同义词回退搜索

若 Aone 项目或阿拉丁测试计划任一维度首次搜索未返回候选，Skill 会自动按 `config.yaml` 中 `matching.synonyms` 定义的同义词表对用户输入进行扩展，并再次搜索。例如用户输入「币种国际化」，同义词表可能包含「价美」、「价格美化」、「多币种」等，扩展后会分别用这些关键词再次搜索。

扩展搜索后：
- 若找到候选，展示给用户确认，例如：
  ```
  未通过「币种国际化」匹配到 Aone 项目，但使用同义词「价美」找到以下候选，请确认：
  1. FY26_淘海外_手淘币种国际化战役（ID: 2117281）

  未通过「币种国际化」匹配到阿拉丁测试计划，但使用同义词「价格美化」找到以下候选，请确认：
  1. 价美-功能测试（ID: TP-1001）
  ```
- 若所有同义词扩展后仍未找到候选，进入手动输入流程。

#### 未匹配到项目或测试计划时的手动输入

若同义词回退搜索后仍未返回候选，必须向用户反馈，并提供手动输入入口。示例：

```
未在 Aone 中匹配到与「会员积分兑换」相关的项目（已尝试同义词扩展），请确认项目名称是否正确，或手动输入 Aone 项目 ID（如 2158868）：
未在阿拉丁中匹配到与「会员积分兑换」相关的测试计划（已尝试同义词扩展），请确认需求名称是否正确，或手动输入阿拉丁测试计划 ID，或粘贴已有的钉钉文档/语雀文档链接作为测试进度来源：
```

用户可选择：

- 重新输入关键词再次搜索。
- 手动输入对应 ID。
- **粘贴已有的钉钉文档或语雀文档链接**（当阿拉丁测试计划无法获取时的首选 fallback；Agent 负责读取并解析链接中的测试进度，不再创建新的数据收集文档）。
- 跳过该维度（仅当该维度非必填时允许跳过；Aone 项目 ID 与阿拉丁测试计划 ID 为生成日报的关键参数，原则上不允许跳过）。

确认后同样记录上述列表形式的标识（`aone_project_ids`、`aladdin_test_plan_ids` 等，均可含多个）；若用户提供文档链接，则额外记录 `test_progress_doc_url`。

#### ione 项目 ID 长时间模糊搜索无果时的处理（缺陷来源回退）

**触发条件**：用户输入的是项目名称，Agent 在 ione 平台上按名称（含同义词扩展）反复模糊搜索、翻页到底后仍未匹配到对应的 ione 项目 ID / 需求时，进入本流程。**不得**擅自跳过或凭空编造 ID，必须先向用户反馈并让其选择。

1. **第一次反馈，向用户提供三选一**：

   ```
   在 ione 平台上按「<用户输入的项目名称>」反复模糊搜索仍未匹配到对应的项目 ID。请选择接下来的处理方式：
   1. 跳过（本次不纳入 ione 缺陷来源）
   2. 由你手动输入 ione 项目 ID
   3. 去指定缺陷空间按项目名称搜索匹配的缺陷
      （https://aione.alibaba-inc.com/v2/project/2065465/bug#viewIdentifier=a6498b4c967d6d4e928ff630）
   ```

2. **若用户选择「去指定缺陷空间搜索」**：用 builtin_browser 打开该缺陷空间 `https://aione.alibaba-inc.com/v2/project/2065465/bug#viewIdentifier=a6498b4c967d6d4e928ff630`，以用户输入的项目名称为关键词，**模糊搜索最近三个月内（当前日期往前推 3 个月）创建或更新的、与之匹配的缺陷**；须翻页 / 滚动到底，把命中的全部缺陷收集齐后再停止，不得只取首屏。命中的缺陷作为该项目的缺陷数据来源纳入后续统计。

3. **若在缺陷空间内仍未搜到匹配缺陷**：再次反馈给用户，让其在以下三项中重新选择——跳过 / 手动输入 ione 项目 ID / 继续在该缺陷空间内搜索缺陷（可更换或放宽关键词后重搜）。如此循环，直到用户明确跳过、给出 ID，或搜到匹配缺陷为止。

4. 上述任一路径完成（跳过 / 拿到 ID / 从缺陷空间取到缺陷）后，**后续逻辑仍按既有流程执行**（拉取数据、合并去重、计算风险、生成日报、发送等）。

### 4. 拉取数据

- 用 builtin_browser 打开已登录的 AAT 计划页 `https://aat.alibaba-inc.com/page/jointDetail?tabKey=30&id=<planId>&classify=tp`，reload 后对 `body.innerText` 用正则 `执行成功率[:：]?\s*([0-9.]+%)\s*[（(]?\s*(\d+)\s*[/／]\s*(\d+)` 提取执行成功率与已执行/总数；多个计划分别抓取后汇总总数、已执行、通过、失败、阻塞、跳过。（内网认证页无法用 WebFetch，须用已登录 tab；`get_page_text` 常只返回需求描述，须 reload 后读 `body.innerText`。）
- 若阿拉丁测试计划无法获取（无候选、页面异常、计划已归档/删除等），**必须优先请用户提供已有的钉钉文档或语雀文档链接，由 Agent 直接解析该文档中的测试进度，禁止主动创建新的测试数据收集文档，也禁止直接留空或使用占位文案**。用户粘贴链接后，Agent 读取文档并按以下规则解析：
  1. **提取量化进度（可选）**：若文档中明确包含用例执行数据，提取总数、已执行、通过、失败、阻塞、跳过等字段；表格形式的指标优先按表头-表身解析，文本形式的指标优先匹配「用例总数 / 已执行 / 未执行 / 通过 / 失败 / 阻塞 / 执行成功率 / 用例通过率」等关键字。
  2. **描述测试内容（核心）**：当输入来源为测试文档时，**严格只描述文档中实际记录的测试内容**，包括测试范围、覆盖模块、执行策略、已完成的测试项、遗留或待执行的测试项。**禁止发散 Agent 自身对文档内容的看法、推测、评价或额外解读**，只忠实转述文档原有信息。
  3. **识别难点与耗时点**：仅提炼文档中**明确提到**的难测、费时、阻塞、风险高或需要额外资源的测试内容（如复杂链路、跨境场景、多币种兼容、回归范围大、环境依赖等）；文档未提及的不得自行脑补。
  4. **提炼风险与问题**：仅从文档的「风险」「问题」「卡点」「明日待办」等章节提取当前阻塞项、待确认点和下一步行动；文档未提及的不得自行发散。
  5. **归档与沉淀**：将文档中散落的测试记录按模块/阶段/风险维度归类，形成可复用的测试进度摘要，保持客观描述，不加入主观判断。
  6. 将上述信息整理为 `test_progress_notes`，写入日报「测试进度」单元格；若无法提取到量化进度，则在「测试进度」中直接展示文档来源的测试内容、覆盖范围，并明确标注「数据来自钉钉/语雀文档」。
  7. **文档更新时间作为测试结束时间回退**：若未获取到阿拉丁测试计划，但用户提供了钉钉文档或语雀文档作为测试进度来源，则将该文档的最后一次更新时间视为测试结束时间 `test_end_date`（钉钉文档优先取 `dws doc read` 返回或文档元数据中的 `updatedAt`；语雀文档优先取页面/API 返回中的 `updated_at` / `lastUpdated`）。该时间用于后续「当前日期 ≥ 测试结束时间」的图表渲染判断以及测试周期相关计算。
- 对 `aone_project_ids` 中的**每一个**项目分别调用 coop `search_workitem_by_project`（`projectId` 为该项目 ID，`createdAtFrom` 带时区 ISO8601、`statusList` 过滤），逐项目拉取缺陷后**合并去重**（按 `bug_id`），再统一统计：总数、严重、高、中、低、已关闭、未关闭。每条缺陷保留其所属 `aone_project_id`，用于后续正确拼接缺陷详情页链接。
- 缺陷详情页链接按**每条缺陷所属的项目 ID** 拼接，不假定所有缺陷都属于同一个项目。
- **多项目 / 多计划汇总规则**：`aladdin_test_plan_ids` 中的多个测试计划分别抓取执行数据后，用例总数、已执行、通过、失败、阻塞、跳过按计划求和汇总；`aone_project_ids` 中多个项目的缺陷合并到同一份缺陷集中做分布统计（业务模块 / 开发责任人 / 缺陷类型），生成一份覆盖全部所选项目与计划的日报。风险评估基于汇总后的整体执行率、通过率与未关闭 P0/P1 数量进行。

### 5. 计算风险

根据距离发布日的剩余天数与测试完成度评估风险。默认阈值如下：

| 剩余天数 | 执行率阈值 | 通过率阈值 | 未关闭 P0/P1 阈值 | 风险等级 |
|---------|-----------|-----------|------------------|---------|
| ≤ 1 天  | ≥ 95%     | ≥ 95%     | 0                | 高      |
| ≤ 3 天  | ≥ 80%     | ≥ 90%     | ≤ 2              | 中      |
| ≤ 7 天  | ≥ 60%     | ≥ 85%     | ≤ 5              | 低      |
| > 7 天  | ≥ 30%     | ≥ 80%     | ≤ 10             | 无      |

任意指标低于阈值时，风险等级上浮一档（最高为「高」，「无」仅作为基准）。具体规则与公式见 [reference.md](reference.md)。

### 6. 生成报告

**必须参照本 Skill 内置的 HTML 模板 [`assets/report-template.html`](assets/report-template.html) 生成日报，不得自行改用其它版式。**

生成报告时，必须同时产出并保留以下两种格式，最终一并提供给用户：

- **HTML 文件**：基于 `report-template.html` 渲染生成的完整 HTML 日报，保存到 `outputs/` 目录，文件名建议包含项目名称与日期（如 `<project>_daily_report_<YYYY-MM-DD>.html`）。该文件用于离线查看、邮件附件或二次处理。
- **钉钉文档**：将日报内容写入钉钉在线文档，便于团队协同、评论与分享。写入时需将 HTML 表格结构等价转换为钉钉 jsonml 格式，并按要求插入 Chart.js 图表 PNG。

两种格式的内容必须保持一致；钉钉文档创建/更新完成后，需向用户同时返回 HTML 文件路径和钉钉文档链接。 该模板定义了日报的权威版式：一张 5 列、带合并单元格的表格，包含四个蓝色横幅分区——「■ {项目名称}整体概述」「■ 缺陷情况」「■ 问题记录」「■ 变更卡点」；其中「项目进度」单元格纵向合并覆盖风险等级/风险说明（风险等级为「无」时显示为「进度简述」）/测试进度共 3 行。生成前先读取该模板文件，严格按其结构、列宽、合并方式与蓝色横幅样式来组织内容；渲染时需将模板中的 `{风险说明标签}` 占位符替换为「进度简述」或「风险说明」。

**不包含「今日进展」模块（强制）**：日报不再输出「今日进展」这一行。无论 HTML 还是钉钉文档，「■ 整体概述」分区都只包含风险等级、风险说明/进度简述、测试进度 3 行，其后直接进入「■ 缺陷情况」横幅。禁止再新增或保留「今日进展」标签行与其值单元格。

写入钉钉文档时，由于钉钉文档不渲染原始 HTML `<table>`，需将该 HTML 模板的结构等价转换为钉钉的 jsonml 表格（保留 5 列列宽、rowSpan/colSpan 合并、蓝色横幅 `fill: rgba(0, 96, 255, 0.98)` + 白色加粗文字），再通过 `dws doc create/update --content-format jsonml --fix-jsonml` 写入。

样式映射补充：模板中需要居中的单元格在 jsonml 中通过「`tc` 设 `vAlign: "middle"`（垂直居中）+ 其内 `p` 设 `jc: "center"`（水平居中）」实现。具体包括 `.label-center` 标签单元格（“项目进度”“汇总”“new”“later”），以及 `.cell-center` 单元格——即「■ 整体概述」中「风险等级」「风险说明 / 进度简述」「测试进度」这 3 个**左侧标签格**。其余普通 `.label` 与正文单元格（含上述三行右侧的值单元格）保持 `vAlign: "top"`、`p` 不设 `jc`（默认左对齐）即可。

**「■ 整体概述」三行标签格必须垂直居中 + 水平居中（强制）**：「风险等级」「风险说明（风险等级为「无」时为进度简述）」「测试进度」这 3 个左侧标签单元格，在 HTML 与钉钉文档中都必须水平居中且垂直居中，与「缺陷情况」区的「汇总 / new / later」标签保持一致。HTML 侧标签格用 `class="label cell-center"`（`.cell-center { text-align: center; vertical-align: middle; }`）；钉钉 jsonml 侧对应 3 个 `tc` 设 `vAlign: "middle"` 且其内 `p` 设 `jc: "center"`。**右侧值单元格不居中**：风险等级标识、进度简述/风险说明有序列表、测试进度文本均保持默认左对齐 + 顶端对齐（HTML 值格不加任何居中 class，钉钉 `tc` 保持 `vAlign: "top"`、内部每个 `p`（含带 `list` 属性的列表段落）都不得设 `jc`）。左侧 rowspan 的「项目进度」格维持原有居中样式，不作调整。

按模板需要填充的内容分区包括：

- 执行摘要与总体风险
- 已确认需求的测试进度明细
- 缺陷分布与未关闭严重缺陷
- 风险说明（仅陈述风险数据与事实，不给出主观行动建议）
- 阻塞项与明日待办

#### 问题记录模块：群消息/失败用例补充（强制）

「问题记录」分区用于记录当日测试过程中暴露的阻塞点与待跟进事项。优先补充项目相关钉钉群当天讨论中的卡点与待确认点；当群消息无法获取或未识别到有效卡点时，降级使用失败用例作为问题来源；若两者均无，则直接显示「无」。

**内容口径（强制）：问题记录只客观阐述「具体的问题或卡点」本身——即问题现象、涉及的模块 / 接口 / 场景、影响范围、当前状态等事实信息；禁止 Agent 发表自己的意见、评价、主观判断、原因推测、风险结论或改进 / 修复建议。只如实记录问题是什么、卡在哪里，不做任何延伸解读。**

具体流程如下：

1. **优先拉取群消息（可跳过）**：
   - **群聊模糊搜索**：根据用户输入的项目/需求名称，调用 `dws chat search --keyword "<项目名称>"` 模糊搜索所有相关的钉钉群，获取每个群的 `openConversationId`（cid）。
   - **二次确认（可多选）**：将搜索结果展示给用户，由用户勾选需要拉取消息的群。禁止在未确认的情况下直接拉取消息。展示格式示例：
     ```
     找到以下与「集运三期」相关的钉钉群，请选择需要获取当天群消息的群（可多选）：
     □ 集运三期项目群（cid: xxx）
     □ 集运三期-测试反馈群（cid: yyy）
     □ 集运三期-技术对接群（cid: zzz）
     ```
   - **拉取当天群消息**：用户确认目标群后，对每个选中的群使用 `dws chat message list --group <cid> --time "YYYY-MM-DD 00:00:00" --forward=true` 拉取当天全部消息（从当天 00:00 开始，按时间正序）。如单个群消息量过大，按 group-message-summarizer skill 的分页策略分批拉取、合并去重，确保当天消息完整。
   - **分析卡点与待确认点**：对所选群当天的全部消息进行归类分析，聚焦以下内容：
     - **卡点**：当前阻塞测试或发布的问题、未达成一致的需求/技术点、待确认的决策、资源/环境依赖未到位。
     - **待确认点**：需求细节待确认、修复方案待评审、上线计划/排期待对齐、测试范围待明确。
     - **变更**：需求变更、排期调整、Scope 变动、修复方案变更、上线计划变动。
     - **待办/责任人**：群聊中明确承诺的行动项、负责人与截止时间。
     - **关键文档/链接**：群里分享的需求文档、设计稿、接口文档、测试用例等。

2. **群消息为空或跳过时的降级处理**：
   - 若群搜索无结果、用户选择跳过、群消息拉取失败，或所选群当天无有效讨论/卡点，则**降级**至失败用例总结：
     - 读取阿拉丁测试计划执行结果中的失败用例列表。
     - 提取失败用例的标题、所属模块/计划、失败原因或断言信息。
     - 将失败用例按模块/计划聚类，生成 1~3 条精简问题描述，例如：
       ```
       1. 支付模块存在 2 个失败用例，失败现象集中在跨境运费计算异常。
       2. 下单流程「菲律宾站点税费展示」用例失败，失败接口返回字段与预期不一致。
       ```
   - 若当前测试计划没有任何失败用例（失败数 = 0），则「问题记录」直接显示为：
     ```
     无
     ```

3. **写入问题记录**：将提炼结果写入日报「■ 问题记录」分区。
   - 群消息来源的问题，每条结论必须附带原始聊天记录作为证据，格式遵循 group-message-summarizer skill 的证据引用规范：
     ```markdown
     > 2026-07-20 19:49:58 张三：
     > "@李四 这个接口字段要调整一下，今天下班前给我个方案"
     ```
   - 失败用例来源的问题，采用有序描述格式，概括失败模块、数量与主要现象，不必逐条罗列用例。
   - **每条记录只写「问题/卡点是什么」这一客观事实（现象 + 涉及模块/接口/场景 + 影响范围/当前状态），不得附加 Agent 的意见、评价、原因推测、风险判断或修复/改进建议。**
   - 问题记录采用有序描述格式，第 1 点描述完后换行接着描述第 2 点，依次排列；在 HTML 中用 `<br>` 分隔各条，写入钉钉 jsonml 时以独立段落或换行节点分隔，确保每条在视觉上独占一行。
   - 若群消息和失败用例均未识别到问题，则在问题记录中直接显示「无」。

4. **写入变更卡点**：将群消息分析中归类为「变更」与「待办/责任人」的条目写入日报「■ 变更卡点」分区。
   - **内容口径（强制）**：只客观阐述「发生了什么变更 / 承诺了什么行动」这一事实——变更的类型（需求变更、排期调整、Scope 变动、修复方案变更、上线计划变动）、涉及的模块/接口/场景、影响范围、责任人、截止时间、关键文档链接等；禁止 Agent 加入意见、评价、原因推测或风险判断。
   - 每条结论必须附带原始聊天记录作为证据，格式同「问题记录」：
     ```markdown
     > 2026-07-20 15:32:18 产品-孙七：
     > "@测试 菲律宾税费接口的舍入规则要改成银行家舍入，今天下班前确认影响面"
     ```
   - 变更卡点采用有序描述格式，第 1 点描述完后换行接着描述第 2 点，依次排列；HTML 中用 `<br>` 分隔各条，钉钉 jsonml 中以独立段落或换行节点分隔。
   - 若群消息中未识别到任何变更或待办事项，则在变更卡点中直接显示「无」；群消息未拉取或用户跳过时同样显示「无」，不再使用失败用例做降级（失败用例仅用于「问题记录」的降级）。

5. **异常处理**：
   - 若群搜索无结果，提示用户未找到匹配的群，请提供更准确的群名，或允许用户手动输入群名重新搜索；用户也可选择跳过，进入失败用例降级逻辑。
   - 若用户选择跳过群消息补充，直接进入失败用例降级逻辑。
   - 若某个群消息拉取失败（如权限不足、群不存在），记录失败原因，继续处理其他已选群；全部失败后进入失败用例降级逻辑。
   - 若阿拉丁测试计划无法获取且无失败用例数据，问题记录直接显示「无」。

#### 风险等级颜色标识（强制）

HTML 日报中「风险等级」单元格必须同时展示**彩色实心圆点**和对应文字，颜色映射如下：

| 风险等级 | 圆点颜色 | CSS class | 示例 |
|---------|---------|-----------|------|
| 高 | 红色 | `risk-high` | `<span class="risk-badge risk-high">高</span>` |
| 中 | 黄色/橙色 | `risk-medium` | `<span class="risk-badge risk-medium">中</span>` |
| 低 | 绿色 | `risk-low` | `<span class="risk-badge risk-low">低</span>` |
| 无 | 灰色 | `risk-none` | `<span class="risk-badge risk-none">无</span>` |

模板 `assets/report-template.html` 已内置 `.risk-badge` 及其 `.risk-high / .risk-medium / .risk-low / .risk-none` 样式，通过 `::before` 伪元素渲染 10px 实心圆点。`render_html` 在替换 `{风险等级标识}` 占位符时，必须根据评估出的风险等级输出带对应 class 的 `<span class="risk-badge risk-<level>">...</span>`，禁止仅输出纯文本。钉钉文档 jsonml 中如需要展示同等效果，可插入对应颜色的 emoji 圆点（🔴/🟡/🟢/⚪）或使用带颜色样式的文本节点，但 HTML 版必须使用该样式。

#### 风险说明 / 进度简述格式规范（强制）

「风险说明」单元格的内容必须以**有序列表**呈现：每一点独占一行，第 1 点说完后自动换行再写第 2 点，禁止合并成一段。**HTML 与钉钉文档两种格式都必须保持有序列表**：

- **HTML 版**：用 `<ol class="progress-list">` 承载，每一点是一个 `<li>`，序号由 `<ol>` 自动生成，`li` 内文本不要再手写「1. / 2.」前缀以免重复编号（`render_html` 通过 `_content_to_ol_html` 自动完成拆分与去前缀）。
- **钉钉文档（jsonml）版**：等价转换为**有序列表**，参照「汇总」单元格的做法，将每一点作为列表的一项写入单元格；若钉钉 jsonml 的有序列表节点不可用，则退化为**每点独立段落**且行首带「1. / 2. / 3.」序号并换行，确保视觉上仍是逐条有序、每点独占一行。

根据风险等级分支处理：

- **风险等级为「无」时**：单元格标题改为「进度简述」。内容必须包括：
  1. 整体测试进度百分比（采用「测试执行进度：X%」格式，与「测试进度」单元格一致）与缺陷总数。**测试执行进度 X% 与「共执行 X/Y 条用例」的用例数标绿色**（HTML 用 `<span class="metric-success">`，钉钉 jsonml 对应 leaf `color:"#52c41a"`）。
  2. 项目执行内容分析：结合当日已执行测试用例的覆盖范围、通过/失败分布、阻塞情况，以及当日新提缺陷的数量、严重程度、所属模块/站点等，说明当前测试重点、质量态势和主要发现（其中「共执行 X/Y 条用例」的用例数标绿色）。
  3. 总结简述：**测试执行进度、缺陷总数已在第 1 点描述过，此处不得重复**，仅陈述 P0/P1 缺陷情况等收尾信息。

  示例（内容本身不再重复「进度简述」四字，标签由左侧单元格展示；每一点独立成行；`[绿]` 标注处为绿色）：
  ```
  1. 测试执行进度：[绿]62.5%，缺陷总数：12
  2. 今日共执行 [绿]48/80 条用例，无失败用例，当日新提 3 个 bug，均为中低优先级，分别涉及菲律宾站点税费展示与印尼站点物流状态同步。
  3. 无未关闭 P0/P1 缺陷。
  ```

- **风险等级不为「无」时**：保持原有「风险说明」逻辑，按有序序号列出风险因素与对应数据事实；内容本身不再重复「风险说明」四字，标签由左侧单元格展示。每一点说完后自动换行，再写下一点。单元格中**不要展示测试时间**（测试开始时间、测试结束时间、测试周期、测试起止日期等），不需要把测试时间相关的信息写进日报，也不得加入主观行动建议。**风险说明的正文文字颜色必须与「风险等级」单元格的文字颜色一致**：高＝红色 `#ff4d4f`（`risk-high`）、中＝黄色 `#faad14`（`risk-medium`）、低＝绿色 `#52c41a`（`risk-low`）；HTML 用对应 `<span class="risk-高/中/低">` 包裹每点文字，钉钉 jsonml 对应 leaf `color` 设为同一色值，两种格式保持一致。

  示例格式（假设风险等级为「中」，各点文字为黄色）：
  ```
  1. 测试执行率 45.0%，低于阈值 60.0%。
  2. 未关闭 P0/P1 缺陷 6 个，超过阈值 5 个。
  3. 通过率 80.0%，低于阈值 90.0%。
  ```

#### 测试进度格式规范（强制）

「测试进度」单元格按测试计划数量采用不同展示方式：

- **存在多个测试计划时**：展示所有计划的汇总执行率和总失败用例数，格式为：
  ```
  测试执行进度：{总执行用例/总用例数*100}%，失败用例：{总失败数}
  ```
  例如：
  ```
  测试执行进度：72.9%，失败用例：7
  ```
  无失败用例时，失败用例数展示为 `0`。

- **仅存在一个测试计划时**：展示该计划的已执行/总数，并追加失败用例数，格式为：
  ```
  计划名：已执行数/总数，失败用例：{失败数}
  ```
  例如：
  ```
  集运改版三期-菲律宾：40/40，失败用例：1
  ```

> **颜色（强制）**：「测试进度」单元格中的**测试执行进度百分比**（多计划）与**已执行/总数用例数**（单计划）必须标绿色，与「进度简述」保持一致——HTML 用 `<span class="metric-success">`，钉钉 jsonml 对应 leaf `color:"#52c41a"`；两种格式一致。

不要额外展示通过数、通过率、阻塞/跳过数等信息，保持单元格简洁。

#### 缺陷情况表格结构（强制）

「■ 缺陷情况」分区固定为三行：

1. **汇总**：左侧标签「汇总」，右侧单元格使用**有序列表**（`<ol>`）展示，第 1 点说完后自动换行，第 2 点紧随其后，依次排列。内容顺序固定为：
   1. **缺陷总体统计**：缺陷总数、待解决数、延期数。
   2. **缺陷类型分布**：聚合全部（new + later）缺陷的 `type` 字段，例如 `功能缺陷 6 个，UI 缺陷 3 个，性能问题 2 个，配置问题 1 个`。
   3. **业务模块分布**：按 `module` 字段聚合缺陷个数，例如 `日本站点 1 个，支付模块 3 个，下单模块 2 个`。模块可以是站点、业务域或功能模块，由 ione/Aone 缺陷数据中的标签/模块字段智能识别。
   4. **开发责任人分布**：按 `developer` 字段聚合缺陷个数，例如 `音十 8 个，程君 4 个`。
   5. **未关闭缺陷简约分析**：聚焦高优先级（P0/P1/严重/高优先级）未关闭缺陷，按优先级、模块或根因维度客观陈述数量与分布，避免冗长枚举，不给出主观建议；若需提及主要负责人，仅列举缺陷数量排名前二的负责人，不足两人时不展示该部分。
   6. **统计图**：业务模块分布、开发责任人分布、每日缺陷走势统一放在「汇总」单元格最下方。根据数据量决定使用 Chart.js 图表 PNG 还是文字简述；顺序固定为：
      1. 业务模块分布
      2. 开发责任人分布
      3. 每日缺陷走势（满足条件时）

   **数据颜色标识（强制）**：汇总中的关键数据必须根据状态使用不同颜色标注，颜色通过 HTML `<span class="metric-xxx">` 实现：
   - **红色（`metric-danger`）**：异常数据，例如未关闭高优先级缺陷 > 0、延期数 > 0、失败用例占比过高、风险等级为「高」时的核心指标。
   - **黄色（`metric-warning`）**：数据偏高或需要关注，例如缺陷总数/待解决数超过一般阈值但未达到异常、风险等级为「中」时的核心指标。
   - **绿色（`metric-success`）**：数据正常/进展顺利，例如无延期、无未关闭高优先级缺陷、缺陷总数在合理范围内、风险等级为「低/无」时的核心指标。

   **绘图标准与文字去重（强制）**：业务模块分布与开发责任人分布的渲染条件**各自独立判定**——当缺陷总数 > 5 且**该维度**去重后数量 > 3 时，才将该维度渲染为 Chart.js 图表；此时汇总列表中该维度项只展示「见下方图表」，**禁止再用文字重复列举**。两个维度互不牵连：即使业务模块全部归为「未归类」（仅 1 个分类、不满足 > 3）而不画模块图，只要开发责任人去重后 > 3 且缺陷总数 > 5，开发责任人分布图仍必须照常渲染；反之亦然。若 PNG 生成失败或插入失败，才允许回退为文字汇总，并明确提示用户。

   **HTML 与钉钉两种格式必须一致（强制）**：HTML 版「汇总」单元格用 `<ol class="summary-list">` 有序列表，转换为钉钉文档 jsonml 时**同样必须是有序列表**，两种格式的呈现效果保持一致（逐点编号、每点说完自动换行、下一点紧随其后）。钉钉 jsonml 没有独立的 `ol/li` 标签，有序列表通过**扁平 `p` 节点携带 `list` 属性**实现：每一条汇总文本点（缺陷总体统计 / 缺陷类型分布 / 业务模块分布 / 开发责任人分布 / 未关闭缺陷简约分析）各为一个 `p` 节点，形如 `["p", {"list": {"listId": "<共享ID>", "level": 0, "isOrdered": true, "start": <序号,从1递增>}}, ["span",{"data-type":"text"},["span",{"data-type":"leaf"},"文本"]]]`，同一列表内所有 `p` 共享同一 `listId`、`start` 依次递增。统计图 PNG 图片段落仍保持为普通段落，置于有序列表下方、不带 `list` 属性。
2. **new**：左侧标签「new」，右侧单元格列出今日/当前轮次新缺陷，标题为 Aone 超链接，格式见下方规范。
3. **later**：左侧标签「later」，右侧单元格列出遗留/后续跟进缺陷，标题为 Aone 超链接，格式见下方规范。

new 与 later 均只保留一行，不再额外设置汇总分析行；所有分析总结统一收敛到「汇总」单元格中。HTML 版日报中的 Chart.js 统计图以及写入钉钉/语雀文档的 PNG 图片，均固定放在「汇总」单元格内容最下方，不再单独追加到「■ 缺陷情况」表格之后。

图表渲染条件如下：

- **数据量判断（文字 vs 图表，两维度各自独立）**：业务模块分布、开发责任人分布分别按自己的数据量判断，互不影响。
  - 某维度**数据少**：缺陷总数 ≤ 5，或**该维度**去重后数量 ≤ 3 时，该维度用文字描述，不渲染柱状图。
  - 某维度**数据多**：缺陷总数 > 5 且**该维度**去重后数量 > 3 时，该维度渲染为 Chart.js 柱状图 PNG 插入单元格。
  - 因此可能出现只画其中一张图的情况（例如模块全部「未归类」只画开发责任人图，或反之）。
  - 「每日缺陷走势」仍按下方独立条件决定是否渲染，不受此阈值影响。
- **业务模块分布（数据多时渲染）**：柱状图，按 `module` 字段聚合，例如菲律宾站点、支付模块、下单模块等。x 轴标题为「业务模块类型」，y 轴标题为「bug数量」，y 轴刻度仅显示整数；不显示图例，数据集不设 label，tooltip 仅显示数值。**每个柱形上方必须显示对应实际数值标签**。数据少时以文字形式展示，例如 `日本站点 1 个，支付模块 3 个，下单模块 2 个`。
- **开发责任人分布（数据多时渲染）**：柱状图，按 `developer` 字段聚合。x 轴标题为「开发花名」，y 轴标题为「bug数量」，y 轴刻度仅显示整数；不显示图例，数据集不设 label，tooltip 仅显示数值。**每个柱形上方必须显示对应实际数值标签**。数据少时以文字形式展示，例如 `音十 8 个，程君 4 个`。
- **每日缺陷走势（条件）**：当 `test_duration_days > 5` 且 缺陷总数 > 5 时，额外渲染折线图；横坐标为日期（MM-DD），标题为「测试日期」，纵坐标标题为「bug数量」，y 轴刻度仅显示整数；不显示图例，数据集不设 label，tooltip 仅显示数值。**每个数据点上方必须显示对应实际数值标签**。数据可来自 `daily_bug_counts`（推荐）或从缺陷的 `created_date` 字段按天聚合。数据不足时以文字简述或不展示。

图表作为文字汇总的可视化补充。**若用户要求将日报写入钉钉文档或语雀文档，必须将 Chart.js 图表渲染为 PNG 图片并作为内联图片插入「汇总」单元格最下方，禁止仅保留文字汇总、省略图表或将图片放到表格外部。** 具体生成与插入规范见下方「图表 PNG 生成与文档插入规范」。

#### 图表 PNG 生成与文档插入规范（强制）

当用户选择把日报保存到钉钉文档或语雀文档时，HTML 中基于 Chart.js 的统计图无法被这些在线文档直接渲染，因此必须执行以下步骤：

1. **生成高清 PNG 图片**：
   - 使用本地已安装的 Chrome（如 `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome`）结合 `puppeteer-core` 加载生成的 HTML 日报。
   - 设置高分辨率截图：将页面 `viewport` 的 `deviceScaleFactor` 设为 `2` 或 `3`（例如 `page.setViewport({ width: 900, height: 600, deviceScaleFactor: 2 })`），确保文字、坐标轴、刻度、柱状/折线边缘清晰锐利。
   - 等待 Chart.js 动画渲染完成后，按需对以下 `<canvas>` 分别截图：
     - `#moduleChart`：业务模块分布柱状图，保存为 `module-chart.png`。
     - `#developerChart`：开发责任人分布柱状图，保存为 `developer-chart.png`。
     - `#trendChart`：每日缺陷走势折线图，保存为 `trend-chart.png`。
   - 仅当对应图表满足上述「数据多」或「每日缺陷走势」渲染条件时才生成并插入该 PNG；条件不满足时直接以文字形式展示，不生成空白图片。
   - 若本地无 `puppeteer-core`，优先执行 `npm install puppeteer-core` 安装，不要因此跳过图表插入。
2. **不再保留文字汇总**：
   - 当高清 PNG 成功生成并插入文档后，文档中**不再保留**「图表数据汇总」标题及其文字版分布/走势数据；图片本身即为图表的唯一展示形式。
   - 仅当 PNG 生成失败或插入全部失败时，才允许在文档中回退为文字汇总，并明确告知用户「图表转 PNG 失败，当前以文字汇总展示」。
3. **插入钉钉文档**：
   - 图表 PNG 必须最终落在「■ 缺陷情况」表格的「汇总」单元格内容最下方，顺序为「业务模块分布」→「开发责任人分布」→「每日缺陷走势」（仅渲染时才插入）。
   - 推荐做法：先创建/更新文档，使「汇总」单元格仅包含文字汇总内容；随后对需要渲染为图表的 PNG 调用 `dws doc media insert --node <doc_node_id> --file <png_path> --name <显示名称> -y` 临时插入文档（例如表格末尾），回读后获取各自的内部资源 `src`。
   - 将获取到的 `img` 节点追加到「汇总」单元格的 JSONML 内容最下方，使用 `dws doc block update --node <doc_node_id> --block-id <summary_tc_uuid> --content-format jsonml --no-fix-jsonml --element "$(cat <jsonml_file>)" -y` 精确更新汇总单元格，防止服务端把图片段落移出表格。
   - 更新后使用 `dws doc read --node <doc_node_id> --content-format jsonml` 回读，确认 `img` 节点位于「汇总」单元格内部且 `src` 指向钉钉内部资源路径；同时删除表格外部临时插入的图片节点，避免重复展示。
4. **插入语雀文档**：
   - 通过语雀文档编辑器或对应 API 将需要渲染的 PNG 上传到文档附件，并插入到「■ 缺陷情况」表格的「汇总」单元格内容最下方，顺序同样为「业务模块分布」→「开发责任人分布」→「每日缺陷走势」（仅渲染时才插入）。
   - 上传后重新读取文档内容，确认图片位于「汇总」单元格内。
5. **异常处理**：
   - 若截图模糊或不清晰，应提高 `deviceScaleFactor` 或增大 viewport 后重新截图。
   - 若截图失败，应重试或检查 Chrome 路径/`puppeteer-core` 安装；禁止因截图失败而直接省略图表。
   - 若图片上传/插入失败，保存 PNG 到 `outputs/` 目录并告知用户手动插入。

#### 代码结构说明（参考实现）

本 Skill 的日报生成参考实现由以下两个核心文件组成：

- `assets/report-template.html`：权威 HTML 版式模板，定义 5 列表格、蓝色横幅与单元格合并结构。
- `report_generator.py`：读取模板并按本规范填充内容的 Python 参考实现。

##### 模板占位符

`report-template.html` 中需替换的占位符如下：

| 占位符 | 说明 | 生成函数 |
|--------|------|----------|
| `{项目名称}` | 需求/项目名称 | `render_html` 直接替换 |
| `{风险说明标签}` | 「风险说明」或「进度简述」 | `render_html` 根据风险等级分支 |
| `{风险等级}` | 风险等级（无/低/中/高） | `render_html` 直接替换 |
| `{风险等级标识}` | 带颜色实心圆点的风险等级 HTML，例如 `<span class="risk-badge risk-high">高</span>` | `render_html` 根据风险等级映射为对应 class |
| `{风险说明内容}` | 风险说明或进度简述正文，渲染为有序列表 `<ol class="progress-list">`（每点一个 `<li>`） | `build_risk_description` / `build_progress_brief` → `_content_to_ol_html` |
| `{测试进度}` | 测试计划执行进度文本 | `_build_test_progress_text` |
| `{缺陷情况汇总}` | 「■ 缺陷情况」→「汇总」单元格完整内容 | `_build_summary_cell` |
| `{new缺陷列表}` | new 缺陷 `<li>` 列表 | `_build_bug_list` |
| `{later缺陷列表}` | later 缺陷 `<li>` 列表 | `_build_bug_list` |
| `{问题记录}` | 问题记录正文 | `render_html` 直接替换 |
| `{变更卡点}` | 变更卡点正文（格式与问题记录一致，仅记录变更/待办类事实） | `render_html` 直接替换 |

模板中「■ 缺陷情况」→「汇总」单元格仅保留单一占位符 `{缺陷情况汇总}`，不再拆分多个占位符；所有文字汇总与 Chart.js 图表均由 `_build_summary_cell()` 统一生成并注入该单元格。模板尾部不再保留独立的 `{图表区域}` 或 `<div class="charts">` 块。

##### 核心函数职责

- `evaluate_risk(data, rules)`：根据剩余天数、执行率、通过率、未关闭 P0/P1 数量评估风险等级，返回等级、未达标原因与阈值。
- `build_progress_brief(data)`：风险等级为「无」时生成「进度简述」，包含测试进度百分比、缺陷总数、执行内容分析、总结简述，每点独立成行。
- `build_risk_description(data)`：风险等级不为「无」时生成「风险说明」，按有序序号列出风险因素与对应数据事实，每点结束后换行，不给出主观行动建议。
- `_content_to_ol_html(content)`：将「进度简述 / 风险说明」的多点内容渲染为 HTML 有序列表 `<ol class="progress-list">`，每点一个 `<li>` 由 `<ol>` 自动编号；会去除各行可能已有的行首序号（如 `1.`、`2、`）避免重复编号。`render_html` 用它填充 `{风险说明内容}` 占位符。
- `_build_test_progress_text(data)`：按测试计划数量生成「测试进度」单元格文本；当存在 `test_progress_notes`（文档回退来源）时追加在进度后。
- `_build_summary_cell(data)`：构建「汇总」单元格完整 HTML，负责：
  1. 使用 `<ol>` 有序列表组织汇总内容，每一点独占一行。
  2. 聚合缺陷类型、业务模块、开发责任人分布，并对关键指标（缺陷总数、待解决、延期、未关闭高优先级等）按红/黄/绿规则着色。
  3. 判定文字/图表分支（模块与开发者**各自独立**）：分别计算 `render_module = 缺陷总数 > 5 且 模块去重后 > 3`、`render_developer = 缺陷总数 > 5 且 开发责任人去重后 > 3`；某维度未满足时该项仍用文字描述，满足则列表项写「见下方图表」并生成对应 Chart.js 柱状图。两维度不再共用同一开关，避免一方维度少而连坐掉另一方的图表。
  4. 调用 `_build_unclosed_defect_analysis()` 生成高优先级未关闭缺陷简约分析。
  5. 当 `test_duration_days > 5` 且缺陷总数 > 5 且存在每日数据时，生成每日缺陷走势折线图。
  6. 将需要渲染的图表按固定顺序放在单元格最底部：业务模块分布 → 开发责任人分布 → 每日缺陷走势；图表必须显示数值标签。
- `_build_unclosed_defect_analysis(bugs)`：聚焦高优先级（P0/P1/严重/高优先级/高）未关闭缺陷，按模块聚合并最多提及前两名负责人，返回一段自然语言总结。
- `_build_bug_list(bugs, default_project_id)`：生成 `<li>` 列表，标题自动拼接为 Aone 缺陷详情页超链接，格式为 `序号. <a href="...">标题</a> ｜ @owner`；多项目场景下优先使用每条缺陷自身的 `aone_project_id` 拼接链接，缺失时才回退到 `default_project_id`。
- `_build_issue_notes(data)`：构建「■ 问题记录」分区内容。优先级为：已提供的 `group_chat_notes` / `issue_notes` → 失败用例聚类摘要 → 返回「无」。失败用例优先从 `failed_cases_detail` 明细按模块/计划聚类，无明细时从 `test_plans` 的 `failed` 字段汇总，均无失败时直接返回「无」。
- `_build_change_notes(data)`：构建「■ 变更卡点」分区内容（格式与「问题记录」一致）。优先级为：已提供的 `change_notes` / `group_chat_changes` → 返回「无」。该分区不使用失败用例做降级，仅承载群消息中归类为「变更」与「待办/责任人」的条目；无内容时直接显示「无」。
- `render_html(template_html, data)`：统一替换模板占位符，生成最终 HTML。

##### 图表渲染与数据约定

- 业务模块分布柱状图使用 `<canvas id="moduleChart">`；开发责任人分布柱状图使用 `<canvas id="developerChart">`；每日缺陷走势折线图使用 `<canvas id="trendChart">`。三个 canvas ID 必须与「图表 PNG 生成与文档插入规范」中的截图目标保持一致。
- Chart.js 配置统一隐藏图例（`legend: { display: false }`），数据集不设置 `label`；tooltip 通过 `callbacks.label` 仅返回数值，避免显示 `undefined`。
- y 轴刻度通过 `ticks.callback` 过滤只保留整数，避免显示小数刻度。
- **数值标签防遮挡（强制）**：由于每个柱形/数据点上方都要显示数值标签，y 轴必须预留顶部余量，避免最高的柱形/数据点顶到画布顶部把标签裁切。实现上由 `_suggested_max()` 将 y 轴 `suggestedMax` 固定设为数据最大值 +2（数据为空或 ≤0 时返回 2），确保刻度始终高于最高数据点；同时对所有 Chart.js 图表设置 `layout: { padding: { top: 24 } }`（走势图为 28px），为顶部数值标签留出安全边距。
- **图表数值标签（强制）**：业务模块分布、开发责任人分布、每日缺陷走势在渲染为图表时，必须在每个柱形/数据点上方显示对应的实际数值。推荐通过 Chart.js 插件（如 `chartjs-plugin-datalabels`）或自定义 `afterDatasetsDraw` 实现；禁止仅展示图形而不展示具体数字。
- 数据量少时直接以文字展示，不生成空白 canvas；数据量多时对应汇总列表项只写「见下方图表」，图表容器追加到单元格最底部。

#### 缺陷列表格式规范（强制）

日报缺陷列表（new / later 等）必须遵守以下两条格式要求：

1. **缺陷标题必须是可点击的超链接**，指向该缺陷在 Aone 的详情页，URL 形如 `https://aone.alibaba-inc.com/v2/project/<projectId>/bug/<bugId>`。不要只贴纯文本标题。
2. **负责人花名后面不要附带缺陷编号**。每条缺陷格式为「序号. [缺陷标题](链接)  ｜ @负责人花名」，即以 `｜ @花名` 结尾，不要再加 `（#缺陷号）` 之类的 ID 后缀。

写入钉钉文档（jsonml 格式）时，链接节点内的文本不能是裸文本，否则会报 `bare text not allowed in "a"`。必须把展示文本包在 span 里：

```json
["a", {"href": "https://aone.alibaba-inc.com/v2/project/2065465/bug/84279475"},
  ["span", {"data-type": "text"}, ["span", {"data-type": "leaf"}, "【集运三期】缺陷标题"]]]
```

### 7. 拉取钉钉群列表

通过 DWS（DingTalk Workspace）连接器获取当前用户可发送消息的钉钉群列表。

- 如果 DWS 未安装或未授权，提示用户完成授权：
  - 打开 QoderWork 设置 → 连接器 → DingTalk Workspace
  - 点击「连接」并按提示完成扫码/登录授权
  - 授权完成后重新触发本 Skill
- 如果群列表为空，提示用户确认账号下是否存在群聊。

### 8. 选择目标钉钉群

使用多选形式展示群列表，例如：

```
请选择要发送日报的钉钉群（可多选）：
□ 测试质量群（chat_001）
□ 会员积分兑换项目群（chat_002）
□ 研发团队大群（chat_003）
```

用户确认后，记录所有选中的 `chat_id`。

### 9. 选择发送方式

向用户确认发送方式：

| 方式 | 说明 |
|-----|------|
| 立即发送 | 马上将日报发送到已选中的钉钉群 |
| 固定时间发送 | 指定一个时间，每天在该时间点发送 |
| 连续多日发送 | 指定一个开始时间，并设置连续发送 N 天 |

### 10. 执行发送或创建定时任务

- **立即发送**：调用 DWS 将报告内容发送到每一个选中的群。
- **固定时间发送**：使用 QoderWork 定时任务创建一个每日循环的 cron 任务，每天在同一时间自动执行本流程并发送到已选群。
- **连续多日发送**：创建 N 个一次性定时任务（或在任务逻辑中维护剩余天数计数器），从开始日期起连续 N 天发送；到期后自动停止。

发送失败时，保存 HTML 报告到 `outputs/` 目录并提示用户手动处理；若钉钉文档已生成，也需保留其链接。

## 常见问题

- **拉不到测试进度**：确认浏览器已登录 AAT/阿拉丁，计划页 `id`/`classify` 是否正确，必要时 reload 后再读 `body.innerText`；仍失败则按流程改用用户提供的钉钉/语雀进度文档。
- **拉不到缺陷**：确认 coop 连接器已授权，`aone_project_id` 正确、`createdAtFrom` 为带时区 ISO8601；已归档项目 `get_project_info_by_project_id` 可能 404，但 `search_workitem_by_project` 仍可拉到缺陷。
- **风险等级不符合预期**：调整 `config.yaml` 中 `risk_rules` 的阈值。
- **无法拉取钉钉群列表**：检查 DWS 连接器是否已安装并授权；如未授权，按 Skill 提示完成钉钉登录授权。
- **定时任务未发送**：检查 QoderWork 定时任务状态，确认 Skill 在调度时浏览器仍处于 AAT/阿拉丁登录态，且 coop 与 DWS 连接器仍已授权。
- **群消息发送失败**：单个群发送失败不影响其他群；失败报告会保存到 `outputs/` 目录，可手动补发。

## 扩展资料

- 数据源与配置详细说明：[reference.md](reference.md)
- 执行示例与报告样例：[examples.md](examples.md)
- 配置模板：[config.template.yaml](config.template.yaml)
