# 阿拉丁 + ione 每日测试风险日报 — 详细参考

## 1. 配置说明

### 1.1 配置文件位置

默认读取当前工作目录下的 `config.yaml`，也可通过环境变量指定：

```bash
export TEST_REPORT_CONFIG=/path/to/config.yaml
```

### 1.2 配置字段

> 数据访问不使用任何 Token / API Key：阿拉丁 / AAT 走 builtin_browser 已登录 tab，Aone 走 coop MCP 连接器，钉钉走 DWS 连接器。因此 `config.yaml` 不含 `base_url` / `auth_token` / 接口路径等凭证字段，只保留匹配、风险、报告、调度等参数。

| 字段 | 类型 | 必填 | 说明 |
|-----|------|------|------|
| `aone.project_search_enabled` | boolean | 否 | 是否启用 Aone 项目模糊搜索，默认 `true` |
| `aone.project_id_fallback` | boolean | 否 | 未匹配到 Aone 项目时是否允许手动输入 ID，默认 `true` |
| `matching.strategy` | string | 否 | 匹配策略：`contains`、`similarity`、`both`、`slice`，默认 `slice` |
| `matching.min_similarity` | number | 否 | `similarity` 模式下的最小相似度阈值，默认 0.6 |
| `matching.max_candidates` | number | 否 | 每个平台返回的最大候选数，默认 5 |
| `matching.slice.cn_gram` | number | 否 | `slice` 模式下中文 n-gram 切片长度，默认 2（勿设为 1，会大量误召） |
| `matching.slice.require_all_ascii` | boolean | 否 | `slice` 模式下是否要求查询中的英文/数字片全部命中，默认 `true` |
| `matching.slice.min_cn_hits` | number | 否 | `slice` 模式下中文 n-gram 片至少命中的数量，默认 1 |
| `matching.synonyms` | object | 否 | 同义词扩展表，用于 Aone 项目/阿拉丁测试计划首次未匹配时的二次搜索，结构见下文 |
| `risk_rules` | object | 否 | 自定义风险阈值 |
| `report.title` | string | 否 | 日报标题，默认「测试日报」 |
| `report.timezone` | string | 否 | 报告时区，默认 `Asia/Shanghai` |
| `report.fallback_to_file` | boolean | 否 | 发送失败时是否保存到文件，默认 true |
| `report.output_dir` | string | 否 | 失败报告保存目录，默认 `./outputs` |
| `schedule.default_time` | string | 否 | 默认发送时间，格式 `HH:MM`，默认 `09:00` |
| `schedule.default_consecutive_days` | number | 否 | 连续发送默认天数，默认 5 |

## 2. 需求模糊匹配

### 2.1 匹配流程

1. 接收用户输入的需求名称 `requirement_name`。
2. 分别调用阿拉丁与 ione 的需求搜索接口。
3. 调用 coop `search_project_by_project_name` 搜索 Aone 项目，**逐页翻取（`page` 从 1 递增，按 `perPage` 拉取）直到返回空页，把近一个月内命中的所有项目全部收集齐**，禁止只取第一页或命中第一条即停止。
4. 调用阿拉丁测试计划搜索接口搜索测试计划，**滚动 / 翻页到底，加载出近一个月内命中的全部测试计划后再停止**。
5. 按 `matching.strategy` 对各维度结果进行过滤与排序。
6. **全部维度均保留全部命中结果**（阿拉丁需求 / ione 需求 / Aone 项目 / 阿拉丁测试计划）：全局模糊搜索到的所有候选都原样返回给用户确认，不做数量截断；`matching.max_candidates` 仅可用于展示排序，不得用于截断候选。
7. 若 Aone 项目或阿拉丁测试计划任一维度无候选，按 `matching.synonyms` 同义词表对用户输入进行扩展，并再次搜索该维度。
8. **分两步顺序确认**（含同义词扩展后产生的新候选）：
   - **第一步 · Aone 项目**：把全部 Aone 项目候选（可附带 ione 需求）返回给用户多选确认，等待用户回复。
   - **第二步 · 阿拉丁测试计划**：在 Aone 项目确认完成后，再把全部阿拉丁测试计划候选（可附带阿拉丁需求）返回给用户多选确认。
   - 若经过同义词回退后仍有维度无候选，在对应步骤提示用户手动输入对应 ID 或重新搜索。
9. **两步都确认后**（禁止在确认前拉取任何项目/计划数据；第一步确认前也不得展示测试计划）得到四类标识（项目与测试计划为列表，支持多选）：
   - `aone_project_ids`：第一步选中的 Aone 项目 ID 列表（可含多个项目）。
   - `ione_req_ids`：第一步选中的 ione 需求 ID 列表。
   - `aladdin_test_plan_ids`：第二步选中的阿拉丁测试计划 ID 列表（可含多个计划）。
   - `aladdin_req_ids`：第二步选中的阿拉丁需求 ID 列表。
10. **确认后连续执行（强制）**：两步确认一旦完成，Agent 必须自动、连续地跑完剩余全部步骤——拉取数据 → 计算风险 → 生成报告（HTML + 钉钉文档）→ 拉取钉钉群列表让用户多选 → 按所选方式发送 / 建定时任务，**中途不得停下、不得反问「是否继续」、不得等用户催促**。确认之后仅允许两处必要停顿：钉钉群多选确认、发送方式选择；除此之外不中断。若某数据源暂不可用，按降级规则处理（改用钉钉/语雀文档链接、标注「数据来源异常」等）继续把日报做完，绝不因单一数据源缺失而中止流程。

**搜索范围与穷尽性（强制）：** 搜索范围限定为**近一个月内**（当前日期往前推 1 个月）创建或更新的项目与测试计划；Aone 侧可将 `createdAtFrom` 设为一个月前的带时区 ISO8601 时间，阿拉丁 / AAT 侧按页面时间过滤。所有维度都必须**穷尽全局搜索**——命中一条不代表结束，必须翻遍全部分页 / 滚动到底、确认没有更多结果后才停止，再把全部命中结果返回给用户确认。

### 2.2 匹配策略

| 策略 | 说明 | 适用场景 |
|-----|------|---------|
| `contains` | 需求名称包含用户输入即命中，按匹配位置与长度排序 | 用户输入较完整、名称规范 |
| `similarity` | 计算字符串相似度，过滤低于 `min_similarity` 的结果 | 用户输入可能为简称或缩写 |
| `both` | 先用 `contains` 粗筛，再用 `similarity` 精排 | 候选较多、希望兼顾召回与精度 |
| `slice` | 把查询切成英文/数字片 + 中文 n-gram 片，词序无关地做覆盖判定 | 名称跨中英文、词序颠倒、易缺字，**优先召回**（推荐默认） |

建议默认使用 `slice`（召回率最高）；名称规范、输入完整时可用 `contains`，缩写较多时可用 `similarity` 或 `both`。

### 2.2.1 切片匹配（`slice`，方案 A）

**背景：** 在真实 AAT 测试计划数据上对查询「中文版bonus」做过对比：`contains` 召回 50%（漏掉「中文bonus正向(...)」缺「版」字、「bonus中文版-逆向」词序颠倒两条），`similarity`（difflib ratio，阈值 0.6）仅 25%，降到 0.58 也只有 50% 且引入误召。切片匹配在同批数据上召回 100%、无误召。

**切片规则：**

1. 归一化：查询与候选名称统一转小写。
2. 切片：从**查询**中抽取
   - 英文/数字连续片：正则 `[a-z0-9]+`（如 `bonus`）；
   - 中文 n-gram 片：对每段连续中文按 `slice.cn_gram`（默认 2）滑窗切分（如「中文版」→ `中文`、`文版`）。
3. 判定命中（词序无关）：某候选名称同时满足
   - `require_all_ascii=true` 时，查询里的**每个**英文/数字片都作为子串出现在候选名称中；
   - 命中的中文 n-gram 片数量 ≥ `slice.min_cn_hits`（默认 1）。
4. 中文务必用 2-gram：降到单字（1-gram）会因「中」「版」等公共字把无关计划大量误召（实测准确率从 100% 掉到 67%）。

**相关配置：** `matching.slice.cn_gram`、`matching.slice.require_all_ascii`、`matching.slice.min_cn_hits`。本策略以召回优先，命中结果同样全部返回给用户确认，不做截断。

### 2.3 候选展示格式（分两步顺序展示）

候选**不合并成一屏**，按「先 Aone 项目、后阿拉丁测试计划」分两步展示，每步都支持多选，Aone 项目与阿拉丁测试计划维度均需列出**全部**命中项。

**第一步 · Aone 项目（+ ione 需求）：**

```
已在 Aone 全局搜索到以下项目，请确认要纳入日报的项目（可多选，如 1,3）：
1. 会员积分兑换-菲律宾（ID: 2158868）
2. 会员积分兑换-印尼（ID: 2158869）
3. 会员积分兑换-泰国（ID: 2158870）

同时匹配到的 ione 需求（用于拉取缺陷，可多选）：
1. 【会员积分兑换】功能缺陷（ID: I-2001，状态：进行中，计划发布：2026-07-15）
2. 会员积分兑换-退款场景（ID: I-2002，状态：已完成，计划发布：2026-07-15）
```

**第二步 · 阿拉丁测试计划（+ 阿拉丁需求）**（在用户确认第一步后再展示）：

```
Aone 项目已确认。以下是阿拉丁全局搜索到的测试计划，请确认（可多选，如 1,3；也可粘贴钉钉/语雀文档链接作为测试进度来源）：
1. 会员积分兑换-功能测试（ID: TP-1001）
2. 会员积分兑换-回归测试（ID: TP-1002）
3. 会员积分兑换-兼容测试（ID: TP-1003）

关联的阿拉丁需求（可多选）：
1. 会员积分兑换功能（ID: A-1001，负责人：张三，计划发布：2026-07-15）
2. 会员积分兑换优化（ID: A-1002，负责人：李四，计划发布：2026-08-01）
```

展示内容应包含：平台、序号、需求/项目/计划名称、ID、负责人/状态、发布时间（如有）。

### 2.4 用户确认规则

- **两步顺序确认**：先确认 Aone 项目（第一步），用户回复后再展示并确认阿拉丁测试计划（第二步）；第一步确认前不得展示测试计划，两步都确认前不得拉取数据。
- 若某维度仅有一个高置信度候选（如 `contains` 完全命中且唯一），可直接默认选中，但仍需告知用户。
- 若阿拉丁需求或 ione 需求无候选，提示用户检查关键词或补充更完整的名称。
- 若 Aone 项目或阿拉丁测试计划无候选，必须在对应步骤向用户反馈，并提供以下选项：
  - 重新输入关键词再次搜索。
  - 手动输入对应 ID（Aone 项目 ID 或阿拉丁测试计划 ID）。
  - 跳过该维度（仅当该维度非必填时允许；Aone 项目 ID 与阿拉丁测试计划 ID 为日报关键参数，原则上不允许跳过）。
- **Aone 项目与阿拉丁测试计划均支持多选**：每一步用户都可一次输入多个编号（如 `1,3`）选择多个项目/计划，Skill 会对所选的多项目、多计划分别拉取并汇总数据。
- 确认后记录四类标识（均为列表）：`aone_project_ids`（第一步）、`ione_req_ids`（第一步）、`aladdin_test_plan_ids`（第二步）、`aladdin_req_ids`（第二步）。

### 2.5 同义词回退搜索

当 Aone 项目或阿拉丁测试计划首次搜索未返回候选时，Skill 会按 `matching.synonyms` 配置对用户输入进行扩展，并再次搜索。

#### 配置格式

```yaml
matching:
  synonyms:
    "会员积分兑换": ["积分兑换", "积分商城", "会员积分"]
    "币种国际化": ["价美", "价格美化", "多币种"]
    "集运三期": ["集运改版三期", "集运3期", "集运第三阶段"]
```

- 顶层 key 为用户输入的原始关键词（或需求名称）。
- value 为同义词列表；首次搜索无结果时，依次用列表中的每个同义词替换原关键词再次搜索。
- 命中即停止扩展，展示该同义词下的候选供用户确认。

#### 候选展示规则

- 同义词扩展后产生的新候选需要与原始候选一起展示，并标注来源关键词，例如：
  ```
  Aone 项目匹配结果（使用同义词「价格美化」）：
  1. FY26_淘海外_手淘币种国际化战役（ID: 2117281）
  ```
- 若多个同义词均产生候选，全部列出，由用户选择。
- 若所有同义词扩展后仍未命中，提示用户手动输入 ID 或重新搜索。

### 2.6 ione 项目 ID 长时间模糊搜索无果时的处理（缺陷来源回退）

**触发条件**：用户输入的是项目名称，Agent 在 ione 平台上按名称（含 §2.5 同义词扩展）反复模糊搜索、翻页到底后仍未匹配到对应的 ione 项目 ID / 需求时，进入本流程。**不得**擅自跳过或凭空编造 ID，必须先向用户反馈并让其选择。

1. **第一次反馈，向用户提供三选一**：
   - 跳过（在报告中注明未关联 ione 项目 / 缺陷来源）。
   - 手动输入 ione 项目 ID。
   - 去指定缺陷空间 `https://aione.alibaba-inc.com/v2/project/2065465/bug#viewIdentifier=a6498b4c967d6d4e928ff630` 按项目名称搜索匹配的缺陷。
2. **若用户选择「去指定缺陷空间搜索」**：用 builtin_browser 打开该缺陷空间（已登录 tab），以用户输入的项目名称为关键词，模糊搜索**最近三个月内**创建或更新的匹配缺陷；翻页 / 滚动到底收集全部命中，纳入缺陷数据来源。
3. **若仍未搜到**：再次反馈让用户选择——跳过 / 手动输入 ID / 继续在该缺陷空间搜索（可换关键词），循环直到用户明确跳过、给出 ID，或搜到缺陷为止。
4. 任一路径完成后，**后续逻辑仍按既有流程执行**（拉取执行进度、缺陷合并去重、风险计算、报告生成、问题记录补充等均不变）。

## 3. 平台数据对接

> 数据访问不使用任何 Token / API Key。阿拉丁 / AAT / ione 均为内网认证页，WebFetch 会失败，须用 builtin_browser 的「已登录 tab」访问；Aone 走 coop MCP 连接器。

### 3.1 需求搜索（阿拉丁 / ione，浏览器）

用 builtin_browser 打开已登录的阿拉丁 / ione 页面，按需求名称或 ID 在页面内搜索，读取候选列表。

**读取要点**：

- 内网认证页无法用 WebFetch，须用已登录 tab（`tabs_context` 定位 → `navigate`/`read_page` 带 `tabId`）。
- `get_page_text` 常只返回需求描述，完整内容需 reload 后读 `body.innerText`。
- 从页面提取候选的 `req_id`（需求 ID）、`name`（名称）、`owner`（负责人）、`release_date`（计划发布）、`status`（状态），映射到内部字段。

### 3.2 阿拉丁 / AAT — 用例执行进度（浏览器）

用 builtin_browser 打开已登录的 AAT 计划页，抓取执行进度：

```
https://aat.alibaba-inc.com/page/jointDetail?tabKey=30&id=<planId>&classify=tp
```

**抓取步骤**：

1. `navigate` 到计划页后 reload（内网认证页无法用 WebFetch）。
2. 用 `javascript_tool` 读取 `body.innerText`，正则提取执行成功率与用例数：
   `执行成功率[:：]?\s*([0-9.]+%)\s*[（(]?\s*(\d+)\s*[/／]\s*(\d+)`
   其中第 2、3 个捕获组分别为「已执行 / 总数」。
3. 多个计划分别抓取后，汇总 `total_cases`、`executed_cases`、`passed_cases`、`failed_cases`、`blocked_cases`、`skipped_cases`。
4. 页面上的「测试时间」为计划起止日，仅用于内部 `test_end_date` 判断，**不写入日报「风险说明」**。

**字段映射**：

| 页面字段 | 本 Skill 内部字段 | 说明 |
|-----------|-----------------|------|
| `total_cases` | `total_cases` | 用例总数 |
| `executed` | `executed_cases` | 已执行用例数 |
| `passed` | `passed_cases` | 通过用例数 |
| `failed` | `failed_cases` | 失败用例数 |
| `blocked` | `blocked_cases` | 阻塞用例数 |
| `skipped` | `skipped_cases` | 跳过用例数 |
| `execution_rate` | `execution_rate` | 执行率 |
| `pass_rate` | `pass_rate` | 通过率 |

### 3.3 Aone（coop）— 项目搜索

**建议接口**：

```
coop search_project_by_project_name --name "{requirement_name}"
```

**请求参数**：

| 参数 | 类型 | 说明 |
|-----|------|------|
| `name` | string | 用户输入的项目名称或关键词 |
| `page` / `perPage` | number | 分页参数；**必须从 `page=1` 起逐页递增拉取，直到某页返回空结果，才算搜索完成**，禁止只取第一页 |

> 穷尽拉取：一次调用只返回一页结果，命中若干条不代表全部。必须循环翻页把近一个月内命中的所有项目收集齐后再返回给用户，绝不能搜到第一个项目就停止。近一个月范围可结合项目创建/更新时间过滤。

**期望响应字段**：

```json
{
  "total": 3,
  "items": [
    {
      "project_id": 2158868,
      "project_name": "会员积分兑换项目",
      "owner": "王五"
    }
  ]
}
```

**字段映射**：

| Aone 字段 | 本 Skill 内部字段 | 说明 |
|----------|-----------------|------|
| `project_id` | `aone_project_id` | Aone 项目 ID，用于生成缺陷链接 |
| `project_name` | `aone_project_name` | 项目展示名称 |

### 3.4 阿拉丁 / AAT — 测试计划搜索（浏览器，模糊匹配 + 全量返回）

**必须使用「测试计划列表页」进行模糊搜索，禁止用「计划详情页」当搜索入口。**

- 列表页（正确的搜索入口，可返回多条命中）：`https://aat.alibaba-inc.com/page/jointList?tabKey=tp&tid=26`
- 详情页（仅用于查看单个计划，**不可**用于搜索，只会展示 1 条）：`https://aat.alibaba-inc.com/page/jointDetail?tabKey=30&id=<planId>&classify=tp`

> ⚠️ 单结果 Bug 根因：过去把 `jointDetail?...id=<planId>` 当作搜索入口，它本身就是「某一个计划」的详情页，所以永远只有 1 条。模糊搜索必须走 `jointList` 列表页并读取整张结果表。

**操作步骤（builtin_browser）**：

1. 用 `tabs_context` 找到已登录的 `jointList?tabKey=tp` 标签页；若没有则 `tabs_create` 后 `navigate` 到 `https://aat.alibaba-inc.com/page/jointList?tabKey=tp&tid=26`。
2. 在顶部查询表单的 **「测试计划名称」** 输入框中填入用户项目名称做**模糊匹配**（用 `find` 定位「测试计划名称」输入框，`form_input` 填入关键词），点击 **「查询」** 按钮。搜索维度以名称为主；必要时也可用「Aone项目/需求」筛选。
3. 把底部「每页显示」切到 **50**（最大档），尽量一屏加载更多。
4. **读取整张结果表的全部命中行，并翻页到底**：结果表列为 `ID / 名称 / 测试状态 / 分类 / 测试周期 / 创建 / PTM / PD / PM / 相关测试 / 关联产品需求 / 提BUG空间 / 操作`；表头上方有「测试计划列表 共 N 项」，底部有分页控件（`1 2 3 4 5 6 …`）。逐页点击「下一页」直到最后一页，**逐行**提取每一条计划，禁止只读首屏第一条。
5. 若列表 DOM 提取不稳定，用 `javascript_tool`（参数 `text`，仅表达式）读取结果表 `innerText` 或行节点数组，正则解析每行的 `ID`、`名称`、`测试状态`、`测试周期`。
6. 从命中行提取 `test_plan_id`（ID 列）与 `name`（名称列），并保留 `测试周期`、`测试状态` 供用户辨识。若首次查询无命中，按 `matching.synonyms` 同义词表扩展关键词后**再次在列表页查询**。
7. **将模糊匹配到的全部测试计划原样返回给用户做多选确认，不做数量截断**；`matching.max_candidates` 仅用于展示排序，不得截断候选。用户确认后再拉取数据。

从候选中提取 `test_plan_id`（计划 ID）与 `name`（名称），映射到内部字段。

**字段映射**：

| 页面字段 | 本 Skill 内部字段 | 说明 |
|-----------|-----------------|------|
| `test_plan_id` | `aladdin_test_plan_id` | 阿拉丁测试计划 ID |
| `name` | `aladdin_test_plan_name` | 测试计划展示名称 |

### 3.5 测试计划文档回退（钉钉 / 语雀）

当阿拉丁测试计划无法获取时（无候选、接口异常、计划已归档 / 删除等），Skill 要求用户提供已有的钉钉文档或语雀文档链接，由 Agent 直接读取并解析其中的测试进度数据。此流程仅做文档解析，**禁止主动创建新的测试数据收集文档**。

#### 触发时机

- 阿拉丁测试计划搜索（含同义词回退）均未返回候选。
- 用户选择「提供文档链接」作为替代方案。

#### 用户交互

```
未在阿拉丁中匹配到测试计划，且同义词扩展后仍未命中。
请提供已有的测试进度文档链接（钉钉文档或语雀文档），或手动输入阿拉丁测试计划 ID：
```

#### 文档读取与解析

- **钉钉文档**：使用已登录浏览器 tab 或 DWS `dws doc read --node <nodeId> --content-format jsonml` 读取文档内容；钉钉文档真实内容通常位于 `iframe.contentDocument.body`，表格数据可通过 `dws doc read` 的 jsonml 结果解析，文本数据可通过 `javascript_tool` 提取。
- **语雀文档**：使用已登录浏览器 tab 或 WebFetch 读取文档内容，提取用例总数、已执行数、通过数、失败数、阻塞数等字段，以及文档中关于测试内容、覆盖范围、执行策略的描述。

解析后需完成以下处理：

1. **提取量化进度（可选）**：
   - 表格形式的指标优先按「表头 → 表身」解析，识别 `用例总数 / 已执行 / 未执行 / 通过 / 失败 / 阻塞 / 执行成功率 / 用例通过率` 等关键字。
   - 文本形式的指标优先匹配上述关键字及其对应数值。
   - 计算 `execution_rate = executed_cases / total_cases` 与 `pass_rate = passed_cases / executed_cases`。
2. **总结测试内容**：从文档中提炼：
   - 本次测试覆盖的功能范围与核心链路。
   - 测试策略或执行方式（如回归、冒烟、全量、分阶段等）。
   - 已完成与遗留的测试项。
3. **识别难点与耗时点**：重点提炼文档中提到的比较难测、费时、阻塞、风险高或需要额外资源的测试内容（如依赖外部系统、数据构造复杂、跨端验证、边界场景多、环境依赖等）。
4. **提炼风险与问题**：从文档的「风险」「问题」「卡点」「明日待办」等章节提取当前阻塞项、待确认点和下一步行动。
5. **归档与沉淀**：将文档中散落的测试记录按模块 / 阶段 / 风险维度归类，形成可复用的测试进度摘要。

提炼结果存入内部字段 `test_progress_notes`，用于追加到日报「测试进度」单元格。

#### 文档更新时间作为测试结束时间

当阿拉丁测试计划不可用时，若用户提供钉钉文档或语雀文档链接，Agent 应读取文档元数据并将文档最后一次更新时间作为测试结束时间 `test_end_date`：

- 钉钉文档：优先使用 `dws doc read` 返回或文档属性中的 `updatedAt` 字段。
- 语雀文档：优先使用页面/API 返回中的 `updated_at` / `lastUpdated` 字段。

该 `test_end_date` 用于图表渲染判断（当前日期 ≥ 测试结束时间）以及测试周期相关计算。

#### 期望提取字段

| 字段 | 类型 | 说明 |
|-----|------|------|
| `test_end_date` | string (ISO8601) | 测试结束时间；未获取到阿拉丁测试计划时，取用户提供的钉钉/语雀文档最后更新时间 |
| `total_cases` | number | 用例总数 |
| `executed_cases` | number | 已执行用例数 |
| `passed_cases` | number | 通过用例数（可选） |
| `failed_cases` | number | 失败用例数 |
| `blocked_cases` | number | 阻塞用例数（可选） |
| `skipped_cases` | number | 跳过用例数（可选） |
| `test_progress_notes` | string | 从文档提炼的测试内容、覆盖范围与难点说明，追加到「测试进度」单元格 |

提取后按以下公式计算内部字段：

```
execution_rate = executed_cases / total_cases
pass_rate = passed_cases / executed_cases
```

#### 在日报「测试进度」中的呈现

- 若文档中提取到量化进度，`test_progress_notes` 追加在量化进度之后，用换行分隔。
- 若文档中无量化进度，「测试进度」单元格直接展示 `test_progress_notes`，并前缀「数据来自文档：」。
- 示例：
  ```
  测试执行进度：62.5%，失败用例：2
  文档补充：本期覆盖下单、支付、退款核心链路；跨境运费计算涉及多币种转换，数据构造较复杂，耗费测试时间较多，当前按文档记录已执行 50/80 条用例。
  ```

#### 异常处理

| 场景 | 处理方式 |
|------|---------|
| 链接无法访问 | 提示用户检查链接权限或重新粘贴 |
| 文档中未识别到用例进度表格 | 提示用户提供包含「用例总数 / 已执行 / 失败」等关键字段的文档 |
| 关键字段缺失 | 使用 0 或 null 填充，并在日报「测试进度」单元格标注数据来源异常 |

### 3.6 Aone（coop）— 缺陷数据

缺陷数据从 Aone 拉取，走 coop MCP `search_workitem_by_project`；**对 `aone_project_ids` 中每个项目分别调用一次，再按 `bug_id` 合并去重**：

- `projectId`：`aone_project_ids` 中的**每一个** Aone 项目 ID（逐项目调用）。每条缺陷记录保留其所属 `aone_project_id`，以便按项目正确拼接缺陷详情页链接。
- `subject`：缺陷标题关键字（传空字符串列出全部）。
- `statusList`：按需过滤，如 `["New","Later"]` 统计未关闭缺陷。
- `createdAtFrom`：**必须为带时区的 ISO8601**（如 `2026-07-10T00:00:00.000+08:00`），用于框定当前测试批次。
- 分页 `page` / `perPage`（默认 15），逐页取全。

**Aone Bug 状态码**：New=28、Fixed=29、Closed=33、Later=34。

已归档项目 `get_project_info_by_project_id` 可能 404，但 `search_workitem_by_project` 仍可正常拉到缺陷。Bug JSON 的 `cfs`/`cfs2` 对象含未加引号的数字键（如 `101587:"0"`），须先 `quote_unquoted_keys` 预处理再 `json.loads`。

按优先级 / 状态聚合后映射到内部字段：

| 聚合项 | 本 Skill 内部字段 | 说明 |
|----------|-----------------|------|
| 缺陷总数 | `total_defects` | 缺陷总数 |
| P0 数量 | `p0_defects` | P0 缺陷数 |
| P1 数量 | `p1_defects` | P1 缺陷数 |
| P2 数量 | `p2_defects` | P2 缺陷数 |
| P3 数量 | `p3_defects` | P3 缺陷数 |
| 未关闭（New+Later 等） | `open_defects` | 未关闭缺陷 |
| 未关闭 P0/P1 | `unclosed_p0_p1` | 未关闭 P0/P1 缺陷 |

### 3.7 异常处理

| 场景 | 处理方式 |
|------|---------|
| 浏览器登录态失效（AAT / 阿拉丁跳登录页） | 停止执行并提示用户在已登录的浏览器 tab 中重新登录后重试 |
| coop / AAT 未找到项目或需求 | 记录需求或项目不存在，跳过该项目 |
| 需求搜索无结果 | 提示用户更换关键词，或确认平台中是否已录入该需求 |
| 页面字段缺失 / 正则未匹配 | 使用 0 或 null 填充，并在报告中标注数据来源异常 |
| 页面加载超时 | reload 重试 3 次，仍失败则跳过并告警 |

## 4. 数据聚合逻辑

以用户确认后的四类标识为入参，分别拉取各平台数据，并按 `release_date` 对齐。**项目与测试计划维度均为列表，支持多项目 / 多测试计划**：

- `aladdin_req_ids`：用户选中的阿拉丁需求 ID 列表，用于关联阿拉丁需求。
- `ione_req_ids`：用户选中的 ione 需求 ID 列表。
- `aone_project_ids`：用户选中的 Aone 项目 ID 列表，逐项目拉取缺陷并合并；同时用于按缺陷所属项目生成 Aone 缺陷链接。
- `aladdin_test_plan_ids`：用户选中的阿拉丁测试计划 ID 列表，逐计划抓取执行进度后汇总。

聚合规则：

- **多测试计划**：对 `aladdin_test_plan_ids` 中每个计划分别抓取执行数据，`total_cases / executed_cases / passed_cases / failed_cases / blocked_cases / skipped_cases` 按计划求和，`execution_rate / pass_rate` 基于汇总后的分子分母重新计算。
- **多项目缺陷**：对 `aone_project_ids` 中每个项目分别调用 coop 拉取缺陷，按 `bug_id` 合并去重成一份缺陷集；缺陷分布统计（type / module / developer）、缺陷计数、未关闭 P0/P1 均基于合并后的缺陷集计算。每条缺陷保留 `aone_project_id` 以便正确拼接链接。

聚合后的整体数据结构：

```yaml
requirement_name: "会员积分兑换"
aladdin_req_ids: ["A-1001"]
ione_req_ids: ["I-2001"]
aone_project_ids: [2158868, 2158869, 2158870]
aladdin_test_plan_ids: ["TP-1001", "TP-1002"]
release_date: "2026-07-15"
days_until_release: 23
# 各测试计划明细（用于溯源），最终按 aladdin 汇总参与风险计算
test_plans:
  - plan_id: "TP-1001"
    total_cases: 800
    executed_cases: 600
    passed_cases: 560
    failed_cases: 30
    blocked_cases: 10
    skipped_cases: 0
  - plan_id: "TP-1002"
    total_cases: 400
    executed_cases: 300
    passed_cases: 290
    failed_cases: 10
    blocked_cases: 0
    skipped_cases: 0
aladdin:            # 多计划汇总结果
  total_cases: 1200
  executed_cases: 900
  passed_cases: 850
  failed_cases: 40
  blocked_cases: 10
  skipped_cases: 0
  execution_rate: 0.75
  pass_rate: 0.9444
ione:               # 多项目合并去重后的缺陷统计
  total_defects: 58
  p0_defects: 2
  p1_defects: 5
  p2_defects: 18
  p3_defects: 33
  open_defects: 12
  unclosed_p0_p1: 7
```

缺陷明细中每条记录至少包含以下字段，用于日报「缺陷情况-汇总」的多维度统计：

```yaml
bugs:
  - bug_id: 84279475
    aone_project_id: 2158868
    title: "日本站点运费计算错误"
    severity: "P1"
    owner: "测试花名"
    developer: "音十"
    type: "功能缺陷"
    module: "日本站点"
```

- `aone_project_id`：该缺陷所属的 Aone 项目 ID，用于按项目正确拼接缺陷详情页链接（多项目场景下每条缺陷可能属于不同项目）。

- `type`：缺陷类型，如功能缺陷、UI 缺陷、性能问题、配置问题。
- `module`：业务模块，可以是站点（如日本站点）、功能域（如支付模块）或业务标签。
- `developer`：开发责任人花名，用于汇总每人名下的缺陷数量。

### 4.1 Aone 缺陷链接构造

日报「缺陷情况」中的 new/later 缺陷标题必须链接到 Aone 缺陷详情页。链接格式为：

```
https://aone.alibaba-inc.com/v2/project/{aone_project_id}/bug/{bug_id}
```

其中 `{aone_project_id}` 为**该缺陷所属**的 Aone 项目 ID（多项目场景下按每条缺陷的 `aone_project_id` 分别拼接，不能统一用某一个项目 ID），`{bug_id}` 为 ione 缺陷数据中的对应 Aone bug ID（如 ione 仅返回内部缺陷 ID，需通过映射表或字段转换得到）。

若某条缺陷缺少 `aone_project_id`（例如手动跳过 Aone 项目匹配），则该缺陷标题不再使用超链接，仅展示纯文本，并在报告中以脚注注明「未配置 Aone 项目 ID，缺陷链接暂不可用」。

## 5. 风险评分规则

### 5.1 默认时间窗口阈值

| 剩余天数 | 执行率 | 通过率 | 未关闭 P0/P1 | 基准风险 |
|---------|--------|--------|-------------|---------|
| ≤ 1     | ≥ 0.95 | ≥ 0.95 | 0           | 高      |
| ≤ 3     | ≥ 0.80 | ≥ 0.90 | ≤ 2         | 中      |
| ≤ 7     | ≥ 0.60 | ≥ 0.85 | ≤ 5         | 低      |
| > 7     | ≥ 0.30 | ≥ 0.80 | ≤ 10        | 无      |

### 5.2 风险计算流程

1. 根据 `days_until_release` 确定所属时间窗口。
2. 取该窗口的基准风险等级。
3. 检查三项指标：
   - `execution_rate` 是否低于阈值
   - `pass_rate` 是否低于阈值
   - `unclosed_p0_p1` 是否高于阈值
4. 每出现一项不达标，风险等级上浮一档：无 → 低 → 中 → 高。
5. 最终输出等级与未达标原因列表。

### 5.3 自定义阈值

在 `config.yaml` 中可覆盖默认规则：

```yaml
risk_rules:
  windows:
    - days_max: 1
      execution_rate: 0.95
      pass_rate: 0.95
      unclosed_p0_p1: 0
      base_level: high
    - days_max: 3
      execution_rate: 0.80
      pass_rate: 0.90
      unclosed_p0_p1: 2
      base_level: medium
```

### 5.4 风险说明与进度简述示例

根据风险等级不同，日报「风险说明」单元格内容分支如下。

**统一渲染约束（HTML + 钉钉两种格式都必须遵守）：** 无论「进度简述」还是「风险说明」，正文都必须渲染为**有序列表**——每一点独占一行、逐点自动编号，第 1 点描述完毕后换行再描述第 2 点。HTML 端由 `_content_to_ol_html(content)` 统一渲染为 `<ol class="progress-list">`（每点一个 `<li>`，且会自动去除源文案里已有的「1. / 2.」序号前缀，避免重复编号）；钉钉文档端等价转换为 jsonml 有序列表（对齐「汇总」单元格做法），若目标环境不支持 jsonml 有序列表节点，则退化为带序号「1. / 2. / 3.」的独立段落并在每点之间换行。因此下方示例源文案按「每点一行」书写，最终会被渲染为逐点编号的有序列表。

**风险等级为「无」时：**

单元格标题渲染为「进度简述」。内容要求：第 1 点给出整体测试进度百分比与缺陷总数（**测试执行进度、缺陷总数只在此描述一次，后续点不得重复**）；第 2 点为项目执行内容分析；第 3 点为总结简述，仅陈述 P0/P1 缺陷等收尾信息。其中**测试执行进度百分比与「共执行 X/Y 条用例」的用例数标绿色**（HTML `<span class="metric-success">`＝`#52c41a`，钉钉 jsonml 对应 leaf `color:"#52c41a"`，两格式一致）。例如（源文案，渲染后为 `<ol>` 逐点编号，`[绿]` 处为绿色）：

```
测试执行进度：[绿]62.5%，缺陷总数：12
今日共执行 [绿]48/80 条用例，无失败用例，当日新提 3 个 bug，均为中低优先级，分别涉及菲律宾站点税费展示与印尼站点物流状态同步。
无未关闭 P0/P1 缺陷。
```

**风险等级不为「无」时：**

单元格标题渲染为「风险说明」，内容仅按有序列表逐点陈述风险因素与对应的数据事实（指标与阈值差距），不得展示测试时间，也不得给出主观行动建议或评价。**风险说明正文文字颜色必须与「风险等级」单元格文字颜色一致**：高＝红 `#ff4d4f`（`risk-high`）、中＝黄 `#faad14`（`risk-medium`）、低＝绿 `#52c41a`（`risk-low`）；HTML 用对应 `risk-高/中/低` class 包裹每点文字，钉钉 jsonml 对应 leaf `color` 设为同一色值，两格式一致。例如（源文案，渲染后为 `<ol>` 逐点编号；若风险等级为「中」，各点文字为黄色）：

```
当前执行率 45%，低于窗口阈值 60%。
剩余 5 天，仍有较多用例未完成。
未关闭 P0/P1 缺陷 6 个，超出阈值 5 个。
```

## 6. 报告模板

日报的**唯一权威版式**是本 Skill 内置的 HTML 模板 [`assets/report-template.html`](assets/report-template.html)。生成日报时必须先读取该模板，按其结构（5 列表格、4 个蓝色横幅「整体概述 / 缺陷情况 / 问题记录 / 变更卡点」、项目进度 rowspan 合并、缺陷 new/later 分组、缺陷标题为 Aone 超链接）组织内容，不得自行改用其它版式或旧的 Markdown 模板。

**「■ 变更卡点」分区（强制）**：紧跟在「■ 问题记录」分区之后，格式完全一致（蓝横幅 + 跨 5 列单单元格）。内容从当天拉取的群消息中按关键字筛选：**变更类**匹配消息正文中包含「变更」关键字的条目；**卡点类**匹配消息正文中包含「卡」「堵住」「阻塞」任一关键字的条目。每条结论须附带原始聊天记录作为证据（``> YYYY-MM-DD HH:MM:SS 发言人： "内容"``），有序描述格式、HTML 中 `<br>` 分隔、钉钉 jsonml 中以独立段落或换行节点分隔。只客观阐述「发生了什么变更 / 哪里被卡住」这一事实，禁止 Agent 加入意见、评价、原因推测或风险判断。若当天群消息中未匹配到上述任何关键字，则直接显示「无」；群消息未拉取或用户跳过时同样显示「无」。该分区**不使用失败用例做降级**，失败用例仅用于「问题记录」的降级路径。写入钉钉文档时，单元格结构等价转换为 jsonml，蓝横幅 `fill: rgba(0, 96, 255, 0.98)` + 白色加粗文字 + **文字左对齐**（横幅 `p` 节点不得设 `jc`，与 HTML 默认左对齐一致），内容段落保持 `vAlign: "top"`、不设 `jc`。

**不含「今日进展」行（强制）**：「■ 整体概述」分区仅由「项目进度」rowspan 合并的 3 行组成——风险等级、风险说明（风险等级为「无」时为进度简述）、测试进度，之后直接进入「■ 缺陷情况」横幅。HTML 与钉钉文档两种格式均不得出现「今日进展」标签行或其值单元格；如遇历史文档仍存在该行，更新时应一并删除。

**「■ 整体概述」三行标签格必须垂直居中 + 水平居中（强制）**：风险等级、进度简述/风险说明、测试进度这 3 行的**左侧标签单元格**需水平居中且垂直居中，HTML 与钉钉文档两种格式呈现一致，与「缺陷情况」区的「汇总 / new / later」标签风格统一。HTML 中标签格用 `class="label cell-center"`（`.cell-center { text-align: center; vertical-align: middle; }`）；钉钉 jsonml 中对应 3 个 `tc` 设 `vAlign: "middle"`、其内 `p` 设 `jc: "center"`。**右侧值单元格保持默认左对齐 + 顶端对齐**，不加任何居中 class，钉钉侧 `tc` 保持 `vAlign: "top"` 且内部每个 `p`（包括进度简述/风险说明有序列表中带 `list` 属性的段落）都不设 `jc`。左侧 rowspan 的「项目进度」格保持原有 `.label-center` / `vAlign: "middle"` + `jc: "center"` 不变。

HTML 模板在「■ 缺陷情况」表格下方通过 Chart.js 按条件渲染缺陷统计图：当当前日期大于等于阿拉丁测试计划的测试结束时间时输出图表；若未获取到阿拉丁测试计划但用户提供了钉钉/语雀文档作为测试进度来源，则使用文档最后一次更新时间作为 `test_end_date` 进行判断。**业务模块分布、开发责任人分布两张图的绘制条件各自独立判定，互不牵连**：当缺陷总数 > 5 且**该维度**去重后数量 > 3 时才渲染该维度的柱状图（模块按 `module`、开发责任人按 `developer` 聚合）；否则该维度在汇总中用文字描述、不生成图。因此可能只画其中一张（例如模块全部「未归类」时只画开发责任人图，反之亦然）。柱状图 x 轴标题分别为「业务模块类型」/「开发花名」，y 轴标题为「bug数量」，y 轴刻度仅显示整数，且隐藏图例、不设数据集 label、tooltip 仅显示数值。当 `test_duration_days > 5` 且缺陷总数 > 5 时，额外输出「每日缺陷走势」折线图（横坐标为日期 MM-DD、标题为「测试日期」，纵坐标标题为「bug数量」，y 轴刻度仅显示整数，同样隐藏图例、不设数据集 label、tooltip 仅显示数值）。生成器优先使用 `daily_bug_counts` 字段，否则按缺陷的 `created_date` 字段按天聚合。为避免每个柱形/数据点上方的数值标签被画布顶部裁切，所有 Chart.js 图表均设置 `layout: { padding: { top: 24 } }`（走势图为 28px），同时 y 轴 `suggestedMax` 由 `_suggested_max()` 固定设为数据最大值 +2（数据为空或 ≤0 时返回 2），保证刻度始终高于最高数据点。

写入钉钉文档时，需将该 HTML 版式等价转换为钉钉 jsonml 表格（钉钉不渲染 HTML `<table>`），通过 `dws doc update --content-format jsonml --fix-jsonml` 上传。钉钉 jsonml 不支持外部 JS 图表，因此当用户要求将日报写入钉钉文档或语雀文档时，必须生成高清 PNG 图片（建议使用 `deviceScaleFactor: 2` 或更高）并作为内联图片插入文档；PNG 成功插入后不再保留「图表数据汇总」文字汇总，仅在 PNG 生成/插入失败时回退到文字汇总。具体生成与插入步骤见 SKILL.md「图表 PNG 生成与文档插入规范」。缺陷标题超链接的 jsonml 结构见第 9 节「缺陷列表格式规范」。

**「■ 缺陷情况-汇总」单元格的有序列表在两种格式中必须一致**：HTML 版用 `<ol class="summary-list">` 逐点编号；转换为钉钉文档 jsonml 时同样必须渲染为有序列表，呈现效果一致（逐点编号、每点说完自动换行、下一点紧随其后）。钉钉 jsonml 无独立 `ol/li` 标签，有序列表由扁平的 `p` 节点携带 `list` 属性实现：每条汇总文本点（缺陷总体统计 / 缺陷类型分布 / 业务模块分布 / 开发责任人分布 / 未关闭缺陷简约分析）各为一个 `p`，结构形如 `["p", {"list": {"listId": "<共享ID>", "level": 0, "isOrdered": true, "start": <序号,从1递增>}}, ["span", {"data-type": "text"}, ["span", {"data-type": "leaf"}, "文本"]]]`，同一列表内所有 `p` 共享同一 `listId`、`start` 从 1 起依次递增。统计图 PNG 图片段落保持普通段落（不带 `list` 属性），置于有序列表下方。

**HTML ↔ 钉钉 jsonml 严格一致（强制）**：钉钉文档 jsonml 不仅结构要与 HTML 等价，**文本内容和颜色标识也必须逐字对应**，禁止因分别构建而产生偏差：

1. **文本内容一致**：每个单元格的文案（缺陷统计数值、分析结论、进度简述措辞、未关闭缺陷分析文本等）在两种格式中必须完全相同。禁止 HTML 与 jsonml 使用不同代码路径生成文本（例如 HTML 调用 `_build_unclosed_defect_analysis(all_bugs)` 而 jsonml 用另一段逻辑只分析 `new_bugs + later_bugs`）。正确做法：**先生成 HTML（调用 `report_generator.py` 的 `render_html`），再从 HTML 渲染结果中提取每段文本用于构建 jsonml**，确保文字一字不差。
2. **颜色标识一致**：HTML 中通过 CSS class 着色的数值/文本，jsonml 中必须通过 leaf `color` 属性设为等价色值：

   | HTML CSS class | 色值 | jsonml leaf `color` | 场景 |
   |---------------|------|-------------------|------|
   | `metric-danger` | `#ff4d4f` | `"#ff4d4f"` | 缺陷总数、延期数等超阈值 |
   | `metric-warning` | `#faad14` | `"#faad14"` | 待解决数等需关注 |
   | `metric-success` | `#52c41a` | `"#52c41a"` | 执行进度百分比、已执行/总用例数 |
   | `risk-high` | `#ff4d4f` | `"#ff4d4f"` | 风险说明正文（风险等级为高） |
   | `risk-medium` | `#faad14` | `"#faad14"` | 风险说明正文（风险等级为中） |
   | `risk-low` | `#52c41a` | `"#52c41a"` | 风险说明正文（风险等级为低） |

   着色粒度到单个数值而非整段文本：「缺陷总数 `<红>92</红>` 个，待解决 `<黄>4</黄>` 个」中，仅数值着色，文案不着色。jsonml 实现方式：把每段文本拆为多个 `span(data-type:text) > span(data-type:leaf, color:xxx)` 节点拼接。
3. **结构判定一致**：汇总中"见下方图表"（图表渲染为 PNG 时）vs 完整文字列表（不渲染图表时）的判定逻辑在两种格式中必须同步，禁止 HTML 显示图表+文字"见下方图表"而 jsonml 仍展示完整列表，或反之。

## 7. 问题记录模块的群消息卡点/待确认点补充

### 7.1 触发时机

在生成日报的「■ 问题记录」分区前，必须补充项目相关钉钉群当天讨论中的卡点与待确认点。该步骤在测试进度与缺陷数据拉取完成后执行。

### 7.2 群聊模糊搜索

根据用户输入的项目/需求名称，调用 DWS 群搜索接口：

```bash
dws chat search --keyword "<项目名称>"
```

期望返回包含 `openConversationId`（cid）与群名称的列表，例如：

```json
[
  {
    "openConversationId": "cid_xxx",
    "name": "集运三期项目群"
  },
  {
    "openConversationId": "cid_yyy",
    "name": "集运三期-测试反馈群"
  }
]
```

### 7.3 用户二次确认（可多选）

将搜索结果以复选框形式展示给用户，由用户勾选需要拉取当天消息的群：

```
找到以下与「集运三期」相关的钉钉群，请选择需要获取当天群消息的群（可多选）：
□ 集运三期项目群（cid: cid_xxx）
□ 集运三期-测试反馈群（cid: cid_yyy）
□ 集运三期-技术对接群（cid: cid_zzz）
```

禁止在未获得用户确认的情况下直接拉取消息。

### 7.4 拉取当天群消息

对用户确认的每个群，使用以下命令拉取当天全部消息（从当天 00:00 开始，按时间正序）：

```bash
dws chat message list --group <cid> --time "YYYY-MM-DD 00:00:00" --forward=true
```

- `YYYY-MM-DD` 为日报生成当天日期，按 `report.timezone`（默认 `Asia/Shanghai`）计算。
- 若单个群消息量过大，按 group-message-summarizer skill 的分页策略分批拉取、合并去重，确保当天消息完整。
- 多选群时，对每个群分别执行拉取，最后合并分析。

### 7.5 分析维度

对所选群当天的全部消息进行归类分析，重点提取：

| 维度 | 说明 |
|------|------|
| 卡点 | 当前阻塞测试或发布的问题、未达成一致的需求/技术点、待确认的决策、资源/环境依赖未到位 |
| 待确认点 | 需求细节待确认、修复方案待评审、上线计划/排期待对齐、测试范围待明确 |
| 变更 | 需求变更、排期调整、Scope 变动、修复方案变更、上线计划变动 |
| 待办/责任人 | 群聊中明确承诺的行动项、负责人与截止时间 |
| 关键文档/链接 | 群里分享的需求文档、设计稿、接口文档、测试用例等 |

### 7.6 写入问题记录

将提炼结果写入日报「■ 问题记录」分区。每条结论必须附带原始聊天记录作为证据，格式遵循 group-message-summarizer skill 的证据引用规范：

```markdown
> 2026-07-20 19:49:58 张三：
> "@李四 这个接口字段要调整一下，今天下班前给我个方案"
```

**内容口径（强制）**：问题记录只客观阐述「具体的问题或卡点」本身——即问题现象、涉及的模块 / 接口 / 场景、影响范围、当前状态等事实信息；禁止 Agent 发表自己的意见、评价、主观判断、原因推测、风险结论或改进 / 修复建议。只如实记录问题是什么、卡在哪里，不做任何延伸解读。上面「分析维度」中的卡点、待确认点、变更、待办/责任人、关键文档均按此口径原样转述群聊事实，不得加入 Agent 自己的看法。

若当天群聊未识别到明显卡点或待确认点，注明「当日群聊未识别到明显卡点/待确认点」。

### 7.7 异常处理

| 场景 | 处理方式 |
|------|---------|
| 群搜索无结果 | 提示用户未找到匹配的群，请提供更准确的群名，或允许用户手动输入群名重新搜索 |
| 用户选择跳过 | 在问题记录中注明「未补充群消息」 |
| 单个群消息拉取失败 | 记录失败原因，继续处理其他已选群，并在问题记录中注明该群未成功获取 |
| 消息内容为空 | 在问题记录中注明该群当天无消息 |

## 8. 钉钉群选择、发送与调度

### 8.1 DWS 连接器授权

在执行群列表拉取前，确保 DWS（DingTalk Workspace）连接器处于已连接状态。可通过 QoderWork 设置中的 `qoderwork.settings.connector.builtin.dws` 检查状态。

若未授权，引导用户：

1. 打开 QoderWork 设置 → 连接器 → DingTalk Workspace。
2. 点击「连接」并按提示完成钉钉扫码或登录授权。
3. 授权成功后重新触发本 Skill。

### 8.2 拉取钉钉群列表

使用 DWS 或 QoderWork 的 IM 通道能力获取当前用户可发送消息的钉钉群列表。期望返回：

```json
[
  {
    "chatId": "chat_001",
    "name": "测试质量群",
    "channelPlatform": "dingtalk"
  },
  {
    "chatId": "chat_002",
    "name": "会员积分兑换项目群",
    "channelPlatform": "dingtalk"
  }
]
```

### 8.3 多选目标群

通过 AskUserQuestion 或类似多选交互，让用户勾选需要接收日报的群。记录所有选中的 `chat_id`，作为后续发送目标。

### 8.4 发送方式

| 方式 | 行为 |
|-----|------|
| 立即发送 | 调用 DWS 群发接口，将日报发送到所有选中群 |
| 固定时间发送 | 创建每日循环的 cron 定时任务，时间由用户指定 |
| 连续多日发送 | 创建 N 个一次性定时任务，或创建带计数器的单次任务，连续 N 天发送 |

### 8.5 定时任务示例

**固定时间每日发送**：

```json
{
  "name": "daily-test-risk-report-fixed",
  "schedule": {
    "kind": "cron",
    "expr": "0 9 * * *",
    "tz": "Asia/Shanghai"
  },
  "payload": {
    "kind": "agentTurn",
    "message": "执行 aladdin-ione-daily-test-report Skill：基于已确认需求生成测试风险日报，并发送到已选钉钉群（chat_001,chat_002）。"
  }
}
```

**连续多日发送**（以连续 5 天、每天 9 点为例）：

- 方案 A：创建 5 个一次性任务，分别在第 1、2、3、4、5 天的 9 点触发。
- 方案 B：创建一个任务，每次执行后将剩余天数减 1，剩余天数大于 0 时重新调度下一次执行。

### 8.6 消息发送

日报正文以钉钉文档（jsonml 表格）形式归档，群消息发送一段简洁通知并附上该文档链接即可。发送失败时记录错误并保存报告到 `outputs/`。

## 9. 安全与合规

- 数据访问全部走已授权通道（浏览器已登录 tab / coop / DWS），不使用任何 Token / API Key，也不在配置文件中保存凭证。
- 日报中不包含用户隐私数据或敏感内部路径。
- 数据拉取失败时，仅输出必要的错误摘要，不暴露完整页面内容或原始响应体。
